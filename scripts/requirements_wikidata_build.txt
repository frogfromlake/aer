# Wikidata alias-index build dependencies (Phase 118).
#
# Used only by scripts/build_wikidata_index.py — runs out-of-tree on a
# GitHub Actions runner via .github/workflows/wikidata_index_rebuild.yml.
# Versions kept in sync with services/analysis-worker/requirements.lock.txt
# so a developer can run the script locally inside the worker venv without
# resolution conflicts. Refresh together with `make deps-refresh`.
requests==2.33.0
tenacity==9.1.4
PyYAML==6.0.2
