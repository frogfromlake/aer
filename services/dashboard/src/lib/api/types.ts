/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Log in with email + password (ADR-040)
         * @description Verifies the email/password against the argon2id hash and, on success, creates a server-side session and sets the opaque `__Host-` session cookie (httpOnly, Secure, SameSite=Strict). No token is ever returned in the body — the browser holds only the opaque cookie (BFF token-handler pattern). Pre-auth endpoint.
         */
        post: operations["postAuthLogin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Log out — revoke the current session (ADR-040)
         * @description Revokes the server-side session and clears the session cookie. Idempotent: returns 204 even if no valid session was present.
         */
        post: operations["postAuthLogout"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The currently authenticated user (ADR-040)
         * @description Returns the user bound to the session cookie. Used by the dashboard to hydrate the current-user store and gate the UI. Returns 401 when there is no valid session (e.g. a machine X-API-Key caller has no user identity).
         */
        get: operations["getAuthMe"];
        put?: never;
        post?: never;
        /**
         * Delete the current user's account (DSGVO right to erasure) (ADR-040)
         * @description Right to erasure (DSGVO Art. 17). Permanently deletes the authenticated user; sessions and pending tokens cascade-delete. Clears the session cookie. Irreversible. Requires a session.
         */
        delete: operations["deleteAuthMe"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/me/export": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Export all data AĒR holds about the current user (DSGVO) (ADR-040)
         * @description Right of access + portability (DSGVO Art. 15 / 20). Returns the complete, privacy-minimal record AĒR stores about the authenticated user. Requires a session.
         */
        get: operations["getAuthMeExport"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/sessions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the current user's active sessions (ADR-040, SEC-005)
         * @description Returns the authenticated user's own active sessions so they can review where they are logged in. Privacy-minimal — no session id is exposed; the session making the request is marked `current`. Returns 401 when there is no valid session (a machine X-API-Key caller has no user identity).
         */
        get: operations["getAuthSessions"];
        put?: never;
        post?: never;
        /**
         * Log out everywhere — revoke all of the current user's sessions (ADR-040, SEC-005)
         * @description Revokes every session for the authenticated user, INCLUDING the current one, and clears the session cookie. Use after losing a device. Returns 401 when there is no valid session.
         */
        delete: operations["deleteAuthSessions"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/accept-invite": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Accept an invitation — set password + record consent (ADR-040)
         * @description Exchanges a single-use invite token for an activated account: the invitee sets their own password and records the responsible-use agreement (LICENSE §3.2.b). On success the account becomes active, the token is consumed, and a session is established (auto-login) — the session cookie is set and the user is returned. Pre-auth endpoint.
         */
        post: operations["postAuthAcceptInvite"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/forgot-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request a password reset (ADR-040)
         * @description Issues a single-use, short-lived password-reset token for the account (if one exists) and hands it to the configured email sender. The response is ALWAYS 202 regardless of whether the email maps to an account — no user enumeration. Pre-auth endpoint.
         */
        post: operations["postAuthForgotPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/reset-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Complete a password reset (ADR-040)
         * @description Exchanges a single-use reset token for a new password. On success the token is consumed and ALL existing sessions for the user are invalidated. Pre-auth endpoint (the token is the credential).
         */
        post: operations["postAuthResetPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/change-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Change password (authenticated, knows current password) (ADR-040)
         * @description Changes the password for the authenticated user after verifying the current password. No email round-trip. On success all OTHER sessions are invalidated; the current session stays valid. Requires a session.
         */
        post: operations["postAuthChangePassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/webauthn/register/begin": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Begin passkey registration (ADR-040)
         * @description Starts a WebAuthn registration ceremony for the authenticated user and returns the opaque credential-creation options. The challenge is held server-side until finish. Requires a session.
         */
        post: operations["postAuthWebauthnRegisterBegin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/webauthn/register/finish": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Finish passkey registration (ADR-040)
         * @description Completes a WebAuthn registration ceremony: verifies the attestation against the stored challenge and persists the credential. Requires a session.
         */
        post: operations["postAuthWebauthnRegisterFinish"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/webauthn/credentials": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the current user's passkeys (ADR-040)
         * @description Returns the authenticated user's registered passkeys (no key material). Requires a session.
         */
        get: operations["getAuthWebauthnCredentials"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/webauthn/credentials/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Delete one of the current user's passkeys (ADR-040)
         * @description Removes a registered passkey by its id. Requires a session.
         */
        delete: operations["deleteAuthWebauthnCredential"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/webauthn/assert/begin": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Begin a passkey assertion / step-up (ADR-040)
         * @description Starts a WebAuthn assertion ceremony for the authenticated user (second- factor step-up) and returns the opaque assertion options. Requires a session and at least one registered passkey.
         */
        post: operations["postAuthWebauthnAssertBegin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/webauthn/assert/finish": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Finish a passkey assertion / step-up (ADR-040)
         * @description Completes a WebAuthn assertion ceremony: verifies the signed assertion against the stored challenge and the user's credential, updating the sign counter. Requires a session.
         */
        post: operations["postAuthWebauthnAssertFinish"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/users": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List users (admin only) (ADR-040)
         * @description Lists all user accounts. Requires the `admin` role.
         */
        get: operations["getAdminUsers"];
        put?: never;
        /**
         * Create an invited user (admin only) (ADR-040)
         * @description Creates an account in the `invited` state and issues a single-use invite token. Returns the accept-invite link (also dispatched through the email sender). Requires the `admin` role. No open self-registration.
         */
        post: operations["postAdminUsers"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/users/{id}/suspend": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Suspend a user (admin only) (ADR-040)
         * @description Suspends an account. The user's sessions stop validating immediately (LICENSE §3.3) — the session check requires an active user. An admin cannot suspend their own account. Requires the `admin` role.
         */
        post: operations["postAdminUserSuspend"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/users/{id}/reactivate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reactivate a suspended user (admin only) (ADR-040)
         * @description Returns a suspended account to the `active` state. Requires the `admin` role.
         */
        post: operations["postAdminUserReactivate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/users/{id}/reset-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Admin-initiated password reset (admin only) (ADR-040)
         * @description Issues a single-use password-reset token for the user and returns the reset link (also dispatched through the email sender). Works without SMTP — the operator delivers the surfaced link manually. Requires the `admin` role.
         */
        post: operations["postAdminUserResetPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/analyses": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List saved analyses visible to the current user (Phase 135)
         * @description Returns the analyses the user owns plus those shared with them, newest activity first. No state blob (see GET /analyses/{id}).
         */
        get: operations["getAnalyses"];
        put?: never;
        /**
         * Save the current analysis (Phase 135)
         * @description Creates a saved analysis owned by the current user from a name + serialized Workbench state.
         */
        post: operations["postAnalyses"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/analyses/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one saved analysis incl. its state (Phase 135)
         * @description Returns the full analysis (with the serialized Workbench state) if the user owns it or is a grantee.
         */
        get: operations["getAnalysis"];
        put?: never;
        post?: never;
        /** Delete a saved analysis (owner only) (Phase 135) */
        delete: operations["deleteAnalysis"];
        options?: never;
        head?: never;
        /**
         * Update a saved analysis (Phase 135)
         * @description Updates name/description/state. Allowed for the owner or a grantee with edit permission.
         */
        patch: operations["patchAnalysis"];
        trace?: never;
    };
    "/analyses/{id}/shares": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List the grantees of an analysis (owner only) (Phase 135) */
        get: operations["getAnalysisShares"];
        put?: never;
        /**
         * Share an analysis with a specific user by email (owner only) (Phase 135)
         * @description Grants the named user readable or editable access. No share-with-all; a forwarded link never grants access.
         */
        post: operations["postAnalysisShare"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/analyses/{id}/shares/{granteeId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Revoke a grantee from an analysis (owner only) (Phase 135) */
        delete: operations["deleteAnalysisShare"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Retrieve aggregated time-series metrics
         * @description Fetches gold-layer metrics for the dashboard's "weather map" visualization. The optional `source` parameter filters to a single source (backward-compatible); `sourceIds` accepts a comma-separated list and is unioned with `source` when both are present. When `normalization` is `zscore` or `percentile` (Phase 115) and the resolved scope spans multiple cultural-linguistic frames, the request additionally requires a deviation-level entry in `aer_gold.metric_equivalence`; absent that, the response is HTTP 400 with a `RefusalPayload` (gate=`metric_equivalence`).
         */
        get: operations["getMetrics"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/available": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List available metric names
         * @description Returns the distinct metric names present in the gold layer for the given time range. Enables frontends to discover metrics dynamically.
         */
        get: operations["getMetricsAvailable"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/correlation": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Pairwise Pearson correlation matrix (Metadata mining x correlation matrix)
         * @description Computes per-bucket means for every metric in the requested set, then Pearson `corr` over each pair. Bucket resolution is fixed at 5 minutes, matching the `metrics` time-series endpoint.
         *     Scope is resolved from the union of `scopeId`, `probeIds`, and `sourceIds`. At least one must be present.
         *     Row cap: 20 000 buckets × metrics.
         */
        get: operations["getMetricCorrelation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/correlation/lead-lag": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Generalised metric lead-lag — lagged cross-correlation of two metrics over time
         * @description Cross-correlates the hourly mean series of two metrics over one scope, across a symmetric lag range — "does xMetric lead yMetric?". Reuses the Phase-124 cross-correlation core (generalises `/probes/{id}/lead-lag`, which is fixed to publication activity). Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, `sourceIds`. A multi-language scope requires a validated equivalence grant for BOTH metrics; absent that, a 400 refusal with a Level-1 alternative (WP-004 §5.2).
         */
        get: operations["getCorrelationLeadLag"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/parallel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-article N-metric matrix for parallel coordinates
         * @description Pivots `aer_gold.metrics` per article into a vector over the requested metric set (≥2), keeping only articles that carry every metric. Backs the parallel-coordinates cell. Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, `sourceIds`. A multi-language scope requires a validated equivalence grant for every metric; absent that, a 400 refusal with a Level-1 alternative (WP-004 §5.2). Capped at maxPoints.
         */
        get: operations["getMetricParallelCoords"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/{metricName}/provenance": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Retrieve provenance metadata for a metric
         * @description Returns the methodological provenance for a single metric: tier classification, algorithm description, known limitations, current validation status, extractor version hash, and cultural-context notes. Static fields are sourced from the BFF's `metric_provenance.yaml` config; dynamic fields (`validationStatus`, `culturalContextNotes`) are resolved against the `metric_validity` and `metric_equivalence` tables.
         */
        get: operations["getMetricProvenance"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/{metricName}/distribution": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-scope distribution of a metric (EDA x ridgeline / violin / density)
         * @description Returns histogram bins plus a quantile summary for the given metric over the window, restricted to the resolved scope (probe or source). Backs the EDA x distributional view-mode cells (ADR-020 §View-mode queries).
         *     Scope is resolved from the union of `scopeId`, `probeIds`, and `sourceIds`. At least one must be present. `segmentBy` returns per-segment payloads alongside the aggregate in a `streams` array (Phase 114).
         *     Row cap: 500 000 rows across all sources and the full window.
         */
        get: operations["getMetricDistribution"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/{metricName}/heatmap": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 2D binning of a metric (EDA x heatmap)
         * @description Two-dimensional aggregation of a metric over the window, bucketed by the requested xDimension and yDimension. `dayOfWeek` and `hour` bin on the metric timestamp; `source` groups by source; `entityLabel` and `language` join against `aer_gold.entities` / `aer_gold.language_detections`.
         *     Scope is resolved from the union of `scopeId`, `probeIds`, and `sourceIds`. At least one must be present. `segmentBy` returns per-segment cell arrays alongside the aggregate in a `streams` array (Phase 114).
         *     Row cap: 100 000 cells across all sources and the full window.
         */
        get: operations["getMetricHeatmap"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics/scatter": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Paired-metric scatter (Metadata mining × visual-channel binding)
         * @description Returns one point per article in the resolved scope, positioned by two per-article metrics (`xMetric`, `yMetric`) with optional `sizeMetric` / `colorMetric` channels. Only articles carrying both position metrics in the window contribute a point; the optional channel values are attached when present. Powers the Phase-131 scatter cell where size / colour / position are bound to chosen metric dimensions, and is the forward-compatible substrate for the Phase-133+ metric-chaining work.
         *     Scope is resolved from the union of `scopeId`, `probeIds`, and `sourceIds` — at least one must be present. Points are ordered by article id and capped at `maxPoints` (response `truncated=true` when the cap bit).
         */
        get: operations["getMetricScatter"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/scope/available-metrics": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Metrics available across a multi-source scope (per-source intersection)
         * @description Returns, for the resolved scope, which metric names have data in Gold for **every** scoped source (`available`) versus only **some** of them (`partial`). Powers the Phase-123c cross-probe guard: PanelControls and the scatter axis/size/colour selectors offer only `available` metrics, so a panel spanning probes with asymmetric capability (e.g. a German SentiWS-only metric on a panel that also holds a French source) can never bind a metric that silently yields empty cells. `partial` carries the per-metric source list so the UI can explain *why* a metric was withheld.
         *     Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, and `sourceIds` — at least one must be present. Availability is computed over the `[windowStart, windowEnd]` window so it matches what a panel would actually render.
         *     This is the "Ist" (observed-from-Gold) view. The complementary Manifest-based "expected-but-missing" silent-failure alarm is deferred (Phase 124).
         */
        get: operations["getScopeAvailableMetrics"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/scope/available-metadata": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Categorical metadata fields available across a multi-source scope (intersection)
         * @description Returns, for the resolved scope, which categorical metadata fields have data in `aer_gold.article_metadata` for **every** scoped source (`available`) versus only **some** of them (`partial`). The categorical analog of `/scope/available-metrics`: the dimension picker offers only `available` fields so a panel spanning probes with asymmetric metadata richness never binds a field that silently yields empty cells; `partial` carries the per-field source list so the UI can explain why a field was withheld.
         *     "Present" means the field has at least one value-row in the window for the source — i.e. selecting it would render non-empty. (The Phase-122f metadata-coverage matrix + `structurallyAbsent` remains the methodological Negative-Space surface; this endpoint is only the picker availability gate.) Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, and `sourceIds`; at least one must be present.
         */
        get: operations["getScopeAvailableMetadata"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metadata/{field}/distribution": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-scope distribution of a categorical metadata field (top-N by article)
         * @description Returns, for one categorical metadata field (e.g. `section`, `author`, `tags`), the top-N values ranked by the number of DISTINCT in-scope articles that carry each value, plus the disclosed long tail (`otherArticles`) and the total distinct-value cardinality (`distinctValues`) so a Top-N truncation is never silent. Backs the Phase-133 categorical cell (grouped-bar / "count per category").
         *     Reads `aer_gold.article_metadata` — one row per (article, field) with an Array(String) value; the query arrayJoin-expands the values and counts distinct articles per value, so a duplicate element within one article never double-counts. Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, and `sourceIds`; at least one must be present. A field that is structurally absent for the scope (no rows) returns an empty distribution — the cell renders Negative Space, not a misleading zero (WP-003 §3.2).
         */
        get: operations["getMetadataDistribution"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metadata/{field}/by-metric/{metric}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Cross-tab of a categorical metadata field against a numeric metric
         * @description For one categorical metadata `field` (e.g. `section`, `author`) and one numeric `metric` (e.g. `sentiment_score_sentiws`, `word_count`), returns — per category value — the number of distinct in-scope articles carrying both, and the mean and spread of the metric among them. Top-N by article count; `distinctValues` discloses the full cardinality.
         *     Joins `aer_gold.article_metadata` (the field, arrayJoin-expanded, FINAL) with `aer_gold.metrics` (the metric, pivoted per article) on `article_id`. Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, `sourceIds`; at least one must be present. A cross-frame (multi-language) request requires a validated equivalence grant for the metric; absent that, a 400 refusal with a Level-1 alternative (WP-004 §5.2).
         */
        get: operations["getMetadataCrossTab"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metadata/sankey": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Alluvial flow across an ordered chain of categorical metadata fields
         * @description For an ordered list of categorical metadata `fields` (≥2, e.g. `article_type,section`), returns the alluvial flow: nodes are category values per layer, links are distinct-article flows between consecutive layers (top-N per pair). Reads `aer_gold.article_metadata` (FINAL). Scope is resolved from the union of `scope`/`scopeId`, `probeIds`, `sourceIds`. No metric normalization is involved (article counts), so no equivalence gate.
         */
        get: operations["getMetadataSankey"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metadata-fields": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Corpus-wide extraction status for the Tier-B / Tier-C metadata fields
         * @description Returns, for every observed Tier-B / Tier-C metadata field, its corpus-wide fill rate (how often AĒR actually populated it), how many sources carry it, and whether it is constant across the whole corpus. Backs the Reflection "metadata fields" surface (Task C), which explains the tier system and why most fields stay empty — structural absence (a publisher choice), not an AĒR extraction defect (WP-003 §3.2). The tier and the prose description are curated client-side; only the live measurements are served here so the page never drifts from the real corpus.
         *     Aggregated over `aer_gold.metadata_coverage` (per-method counts) plus a distinct-value pass over `aer_gold.article_metadata` / `aer_gold.metrics` for the constant flag. Whole-corpus, unscoped — the complementary per-probe view is `GET /probes/{probeId}/metadata-coverage`.
         */
        get: operations["getMetadataFields"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/entities": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Retrieve aggregated named entities
         * @description Fetches named entities (persons, organizations, locations) extracted from processed documents, aggregated by entity text and label. The optional `source` parameter filters to a single source (backward-compatible); `sourceIds` accepts a comma-separated list and is unioned with `source` when both are present.
         */
        get: operations["getEntities"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/entities/cooccurrence": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Entity co-occurrence graph (Network Science x force-directed graph)
         * @description Aggregates `aer_gold.entity_cooccurrences` over the window, returns the top-N edges by summed `cooccurrence_count` plus the union of incident nodes. Backs the Network Science discipline view-mode cell.
         *     Scope is resolved from the union of `scopeId`, `probeIds`, and `sourceIds`. At least one must be present. Each node carries a `presence` field listing the resolved sources where it appears, enabling per-source shading in the force-directed graph (Phase 114).
         *     Edge cap: `topN` (default 50, max 6000 — Phase 125b raised the ceiling for the large-scale renderer). `minWeight` drops edges below a weight floor.
         */
        get: operations["getEntityCoOccurrence"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/entities/cooccurrence/query": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Entity co-occurrence graph with explicit scope-group composition (Phase 122i)
         * @description Aggregates `aer_gold.entity_cooccurrences` over the window for an explicit list of scope groups, returning the top-N edges plus the union of incident nodes. Backs the Multi-Panel Workbench (ADR-034) where a single Cell may merge several `(probeIds, sourceIds)` groups into one network.
         *
         *     Hard caps per request: 100 unique source identifiers and 25 unique probe identifiers across the unioned scope groups. Over-limit requests return 413 with offending counts on the error envelope.
         *
         *     Cross-language merge gate: when the union of scope groups resolves to more than one Language Capability Manifest language, the handler returns 422 `cross_language_merge_unsupported`. BERTopic- adjacent embedding stacks are language-specific (ADR-024) and merged graphs across languages mix incompatible feature spaces; the dashboard surfaces a refusal pointing the user to split- composition or single-language narrowing.
         *
         *     The legacy GET `/entities/cooccurrence` remains for single-scope queries and is unchanged.
         */
        post: operations["postEntityCoOccurrenceQuery"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/topics/distribution": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-scope topic distribution (Episteme x ridgeline / stream-graph)
         * @description Returns the per-language topic distribution over the window for the resolved scope. Backs the Phase 121 `topic_distribution` and `topic_evolution` view-mode cells. Topics come from BERTopic (`aer_gold.topic_assignments`, Phase 120) — fit per language partition, so cross-language `topic_id` comparisons are explicitly meaningless and must not be performed at the rendering layer.
         *     Scope is resolved from the union of `scopeId`, `probeIds`, and `sourceIds`; at least one must be present. The `language` parameter further restricts the response to one language partition. Outliers (`topic_id == -1`) are excluded by default — set `includeOutlier=true` to surface them as an `uncategorised` ridge per the Phase 121 design.
         *     Topic cap: 50 topics by `articleCount` descending.
         */
        get: operations["getTopicDistribution"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/languages": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Retrieve aggregated language detections
         * @description Fetches detected languages aggregated across processed documents. Only rank-1 (top candidate) detections are included in the aggregation. The optional `source` parameter filters to a single source (backward-compatible); `sourceIds` accepts a comma-separated list and is unioned with `source` when both are present.
         */
        get: operations["getLanguages"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sources": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List known data sources with methodology documentation
         * @description Returns the canonical list of data sources configured in AĒR together with optional links to their methodological documentation (e.g. bias profiles under `docs/methodology/`). When `silverOnly=true` (Phase 103) the response is filtered to sources whose `silver_eligible` flag is set, so the dashboard's Silver-layer source picker avoids surfacing sources that the eligibility gate would refuse.
         */
        get: operations["getSources"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sources/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Source detail with Silver-eligibility metadata
         * @description Returns a single source record with its Silver-layer eligibility state and the WP-006 §5.2 review metadata. Backs the dashboard's per-source side panel so reviewers and operators can see *why* a source is (or is not) Silver-eligible without reading Postgres directly.
         */
        get: operations["getSourceById"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/silver/documents": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Silver-layer documents for a source
         * @description Paginated listing of Silver-layer documents (`SilverCore` summaries) for a single source within an optional time window. Subject to the Silver-eligibility gate (Phase 103, ADR-020 §"Silver-layer toggle"): a source whose `silver_eligible` flag is false returns 403 with a RefusalPayload citing WP-006 §5.2.
         */
        get: operations["listSilverDocuments"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/silver/documents/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Silver-layer document detail
         * @description Returns the full Silver envelope for a single article — `SilverCore` (cleaned text included), the source-specific `SilverMeta` blob (per ADR-015 explicitly unstable), and the per-extractor provenance map captured at extraction time. Subject to the Silver-eligibility gate (Phase 103): a non-eligible source returns 403 with a RefusalPayload.
         */
        get: operations["getSilverDocumentDetail"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/silver/aggregations/{aggregationType}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Silver-layer aggregations (distribution / heatmap / correlation)
         * @description Aggregate query over `aer_silver.documents` (Phase 103b projection table) for one Silver-eligible source. The `aggregationType` path parameter selects both the projection field(s) and the shape of the response payload:
         *     * `cleaned_text_length`, `word_count`, `raw_entity_count` — histogram
         *       bins + quantile summary (the `distribution` payload).
         *     * `cleaned_text_length_by_hour`, `word_count_by_source` — 2D binning
         *       (the `heatmap` payload). `word_count_by_source` is degenerate when
         *       filtered to a single source and is intended for whole-probe queries
         *       via Phase 111's source toggle.
         *     * `cleaned_text_length_vs_word_count` — pairwise Pearson correlation
         *       between two projection fields (the `correlation` payload).
         *
         *     Subject to the Silver-eligibility gate (Phase 103, ADR-020 §"Silver- layer toggle"): a non-eligible source returns 403 with a RefusalPayload citing WP-006 §5.2.
         */
        get: operations["getSilverAggregation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/probes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List active probes with emission geometry
         * @description Returns the canonical list of active probes configured in AĒR with their bound sources and emission origin coordinates. Probes are the unit of rendering on the Atmosphere surface (Design Brief §4.2).
         *     No reach information is returned. Where a probe's content is consumed or discursively effective is not measured; only the emission origin(s) are asserted. The frontend renders each emission point as a glowing marker — nothing beyond.
         *     Structural data only. Dual-Register editorial content (emic designation, methodological register) lives on `/content/probe/{probeId}` and is composed on the client.
         */
        get: operations["getProbes"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/probes/{id}/dossier": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Composite Probe Dossier payload
         * @description Returns the data backing Surface II's Probe Dossier landing — the probe's structural identity, function coverage, and per-source cards with article counts and Silver-eligibility state. Composed server-side so the dashboard avoids request waterfalls on first paint (Design Brief §4.2.1, ADR-020 §Backend-Work).
         */
        get: operations["getProbeDossier"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/probes/{probeId}/equivalence": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-probe equivalence summary
         * @description Returns per-metric Level-1 / Level-2 / Level-3 availability (Phase 115). Drives the Probe Dossier "what comparisons are valid here" panel. With the optional `comparedTo=<otherProbeId>` parameter (Phase 124) the scope is the UNION of both probes' sources, so the matrix reports what is valid for the cross-probe pair — e.g. the temporal Level-1 grant reports `validated`.
         */
        get: operations["getProbeEquivalence"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/probes/{probeId}/lead-lag": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Cross-probe temporal lead-lag
         * @description Returns the lagged cross-correlation of hourly publication activity between `probeId` (reference) and `comparedTo` (compared), over ±`maxLagHours`. A relational, cross-cultural artefact authorised ONLY by a temporal Level-1 equivalence grant covering both probes' languages (WP-004 §6.3, Appendix B); otherwise the response is 400 with the RefusalPayload fields (gate=`metric_equivalence`).
         *     Phase 124 minimal form: the signal is publication activity and the gate is the temporal grant. Phase 125 generalises this to arbitrary metric series and a fully-parameterised public endpoint — build on this endpoint, do NOT re-implement it.
         */
        get: operations["getProbeLeadLag"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/probes/{probeId}/metadata-coverage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-source-per-field metadata coverage for a probe
         * @description Returns the per-source metadata-coverage matrix for every source bound to the probe. Backs the Probe Dossier metadata-coverage panel and the field-level Negative-Space rendering (Design Brief §7.7, WP-003 §3.2). Each field carries a per-extraction-method count, a population rate, and a `structurallyAbsent` flag that fires when ≥ 50 articles observed in the last 30 days yielded 0 % population — the threshold that distinguishes "publisher chose not to emit" from "sampling variance".
         */
        get: operations["getProbeMetadataCoverage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sources/{id}/articles": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Paginated article listing for a source
         * @description Returns a paginated list of processed documents for a source, optionally filtered by time window, language, sentiment band, and entity match. Powers the article browser inside the Probe Dossier (Design Brief §4.2.1). Cleaned text and full provenance are not included — fetch `/articles/{id}` for L5 Evidence.
         */
        get: operations["getSourceArticles"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sources/{sourceId}/metadata-coverage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-field metadata coverage for a single source
         * @description Returns the metadata-coverage matrix for one source — the single-source equivalent of `/probes/{probeId}/metadata-coverage`. Backs the per-source dossier surface and any future single-source deep-dive that needs the publisher's emission posture. See WP-003 §3.2 and Design Brief §7.7 for the methodological frame.
         */
        get: operations["getSourceMetadataCoverage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sources/{sourceId}/discovery-coverage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-channel discovery telemetry for one source
         * @description Returns the per-source-per-channel discovery telemetry recorded by the crawler's Phase 122g telemetry layer (`crawler_discovery_runs`). Sibling to `GET /sources/{sourceId}/metadata-coverage` — both surface a publisher's emission posture as a queryable runtime signal.
         *     The payload reports per-channel URL counts for the most recent discovery pass alongside a trailing-window average and the source-level underflow-alert state (Phase 122g two-strike gate). Consumers (dashboard panel, downstream analysis) read this to contextualise corpus-volume comparisons that would otherwise be contaminated by discovery-surface asymmetries — see WP-003 §3.2 and Probe-0 Structural Bias #8.
         *     Recorded in ADR-031 (DiscoveryProtocol Contract for Multi-Channel Source Discovery).
         */
        get: operations["getSourceDiscoveryCoverage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/articles/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * L5 Evidence — article detail with k-anonymity gate
         * @description Returns the cleaned text, Silver metadata, and per-extractor provenance for a single article. Subject to a k-anonymity gate per WP-006 §7: if the article's aggregation group for the referenced metric is below the configured threshold (k = 10 by default), the BFF returns 403 with a methodological refusal payload instead of the article body. This is the operational expression of "observation over surveillance" at the article level — individual visibility is conditioned on aggregate cover.
         */
        get: operations["getArticleDetail"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/articles/{id}/revisions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-article revision chain for L5 Evidence
         * @description Returns the ordered revision chain for one article (Phase 122d.0). Each entry is one detected revision — a Wayback CDX snapshot or a publisher-side republication-trigger event. The dashboard's L5EvidenceReader renders the list below the article body so the operator can scrub the silent-edit timeline alongside the cleaned text.
         *     The endpoint is subject to the Silver-eligibility gate that governs `GET /articles/{id}`: a source whose `silver_eligible=false` returns 403 with a methodological refusal payload (`gate=silver_eligibility`). Articles with no detected revisions return `revisions: []` and a non-empty `lookupStatus` (`no_snapshots`, `disabled`, etc.).
         */
        get: operations["getArticleRevisions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/articles/{id}/revisions/{revisionIndex}/diff": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-pair paragraph-aligned diff between two snapshots
         * @description Returns the paragraph-aligned diff between revision `revisionIndex-1` and `revisionIndex` for one article (Phase 122d.1 / ADR-032 amendment). Subject to the same Silver-eligibility gate as `GET /articles/{id}` and `GET /articles/{id}/revisions`. A `revisionIndex=0` request returns 404 — the chain head has no predecessor to diff against.
         *     The diff is produced by the worker's periodic revision-diff sweep (Wayback snapshot fetch → trafilatura re-extraction → paragraph- aligned diff). If the sweep has not yet processed this pair the endpoint returns 404 with a `pending` message; the dashboard surface should render this as a "diff not yet computed" notice.
         */
        get: operations["getArticleRevisionDiff"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/revisions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Aggregated silent-edit revision activity for a probe or source
         * @description Returns aggregated revision-activity rows over `aer_gold.article_revisions` (Phase 122d.0 / ADR-032). Each entry is one (source, bucket) cell; `byTrigger` distinguishes Wayback CDX snapshots from publisher-side republication-trigger events. Backs the `revision_activity` (Aleph snapshot) and `revision_timeline` (Episteme over-time) cells.
         *     The endpoint is read-only and follows the same scope grammar as `/entities` and `/metrics`. Sources with zero rows in the window do NOT appear in `entries`; the dashboard renders absences from the scope membership.
         */
        get: operations["getRevisionActivity"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/revisions/discourse-shift": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Silent-edit discourse-shift trajectory for a probe or source
         * @description Returns the aggregated discourse shift produced by silent edits over `aer_gold.article_revisions` (Phase 122d.3). Each entry is one (source, bucket) cell rolling up the re-extraction deltas (`sentiment_delta`, `topic_shift_score`, entity add/remove) across the edits whose deltas were computed. Backs the `revision_discourse_shift` Episteme trajectory cell.
         *     Read-only; same scope grammar as `/revisions` and `/metrics`. Only real edits with `deltas_computed=true` contribute — identical re-archivals and pending/partial rows are excluded so the averages are never polluted by default zeros. Sources with no such edits in the window do NOT appear in `entries`.
         */
        get: operations["getRevisionDiscourseShift"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/revisions/edit-clusters": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Coordinated cross-source silent-edit clusters for a probe or source
         * @description Returns coordinated-edit clusters over `aer_gold.article_revisions` (Phase 122d.3, Rhizome). A cluster is a (time bucket, entity) that ≥ `minSources` distinct sources each silently added or removed within the bucket — a cross-source temporal coincidence on the same entity. Backs the `revision_edit_clusters` Rhizome cell.
         *     Read-only; same scope grammar as `/revisions`. Only computed-delta edits contribute. The scope must resolve to ≥ 2 sources for a cluster to exist; a single-source scope returns an empty list. The clustering is a disclosed temporal coincidence, never a causal claim (WP-003 §5).
         */
        get: operations["getRevisionEditClusters"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/revisions/articles": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List articles with revisions for the Workbench drill-down
         * @description Returns a paginated list of articles that have at least one revision in the active window (Phase 122d.1 / ADR-032 amendment). Powers the Workbench drill-down from a `revision_activity` bar or a `revision_timeline` bucket-point to the underlying article list. Cursor-based pagination identical to `/sources/{id}/articles` so the dashboard re-uses the same pagination primitive.
         */
        get: operations["getRevisionsArticles"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness probe
         * @description Returns 200 if the service process is alive. Used by Kubernetes liveness probes.
         */
        get: operations["getHealthz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/readyz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Readiness probe
         * @description Returns 200 if all dependencies (ClickHouse) are reachable. Used by Kubernetes readiness probes.
         */
        get: operations["getReadyz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/content/{entityType}/{entityId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Dual-Register content for an entity
         * @description Returns the semantic and methodological register text for a single entity (metric, probe, discourse function, refusal, view-mode cell, empty-lane invitation, open research question, or primer). Content is sourced from versioned YAML files under `services/bff-api/configs/content/` and loaded at startup. The `locale` query parameter selects the language; defaults to `en`. Returns 404 if no content entry exists for the requested entity and locale combination.
         */
        get: operations["getContent"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * @description Validation status derived from the metric_validity table. "unvalidated" if no entry exists, "validated" if a current entry exists with valid_until in the future, "expired" if the most recent entry has valid_until in the past.
         * @example unvalidated
         * @enum {string}
         */
        ValidationStatus: "unvalidated" | "validated" | "expired";
        /**
         * @description Temporal aggregation resolution. "5min" is computed at query time from `aer_gold.metrics` (raw, 365-day TTL); "hourly" / "daily" / "monthly" resolve to pre-aggregated AggregatingMergeTree materialized views (`metrics_hourly` 365 d TTL / `metrics_daily` 1825 d TTL / `metrics_monthly` no TTL) activated in Phase 122c per WP-005 §5.4. "weekly" rebins the daily MV via `toStartOfWeek` at query time. The enum and request shape are unchanged from Phase 66; the routing is invisible to the client.
         * @example hourly
         * @enum {string}
         */
        Resolution: "5min" | "hourly" | "daily" | "weekly" | "monthly";
        /**
         * @description The level of cross-cultural equivalence established for this metric. "temporal" means only temporal patterns are comparable, "deviation" means deviation from baseline is comparable, "absolute" means raw values are directly comparable across contexts.
         * @example deviation
         * @enum {string}
         */
        EquivalenceLevel: "temporal" | "deviation" | "absolute";
        /** @description Structured equivalence status for a metric (Phase 115). Supersedes the bare-string `equivalenceLevel` field on `/metrics/available`, which is retained for one release cycle as a deprecated alias so existing dashboard URL state and tests continue to load. */
        EquivalenceStatus: {
            /** @description The highest equivalence level established for this metric. Values mirror the `EquivalenceLevel` enum (`temporal`, `deviation`, `absolute`). Null when no equivalence entry exists. Kept as a plain string here rather than an `allOf`-wrapped enum because nesting this schema as an array item generates broken codegen for the inline enum. */
            level?: string | null;
            /** @description Reviewer attribution as recorded on the `aer_gold.metric_equivalence` row. Points back to the full Postgres `equivalence_reviews` record. */
            validatedBy?: string | null;
            /**
             * Format: date-time
             * @description ISO-8601 timestamp of the most-recent equivalence grant.
             */
            validationDate?: string | null;
            /** @description Concise methodological-rationale summary (≤ 280 chars), mirrored from the ClickHouse `metric_equivalence.notes` column. Empty string when no notes were recorded — the read-side default, not an error state. */
            notes: string;
        };
        AvailableMetric: {
            /**
             * @description The name of the metric.
             * @example word_count
             */
            metricName: string;
            /**
             * @description Human-friendly label for the metric in the requested `locale` (Task B), e.g. "Sentiment Score · BERT Multilingual" for `sentiment_score_bert_multilingual`. Resolved from the per-locale content catalogue; null when the metric has no catalogue entry yet — the client then falls back to `metricName` so a newly-added extractor never renders a blank label.
             * @example Word Count
             */
            displayLabel?: string | null;
            validationStatus: components["schemas"]["ValidationStatus"];
            /**
             * @description The etic construct this metric maps to in the equivalence registry (e.g., "evaluative_polarity"). Null if no equivalence entry exists.
             * @example null
             */
            eticConstruct?: string | null;
            /**
             * @deprecated
             * @description DEPRECATED (Phase 115): superseded by `equivalenceStatus.level`. Retained for one release cycle so existing dashboard URL state and tests do not break in the same commit. Mirrors `equivalenceStatus.level`.
             */
            equivalenceLevel?: components["schemas"]["EquivalenceLevel"] | null;
            /** @description Structured equivalence status (Phase 115) — level, validatedBy, validationDate, and `notes` rationale summary mirrored from `aer_gold.metric_equivalence.notes`. Null if no equivalence entry exists for this metric. */
            equivalenceStatus?: components["schemas"]["EquivalenceStatus"] | null;
            /** @description The finest temporal resolution at which this (metric, source) pair yields statistically meaningful aggregates. Derived from a static BFF config map seeded by Probe 0 publication rates. Null if no heuristic has been recorded for the metric-source pair. */
            minMeaningfulResolution?: components["schemas"]["Resolution"] | null;
        };
        /** @description Per-metric equivalence availability for one probe, or for a probe pair when `comparedTo` is set (Phase 124). Drives the Probe Dossier "what comparisons are valid here" panel — making the methodological boundary legible up front, before the user encounters a refusal. With `comparedTo`, the evaluated scope is the union of both probes' sources, so the matrix reflects the cross-probe pair (e.g. the temporal Level-1 grant reports as validated). */
        ProbeEquivalenceSummary: {
            /** @description The probe whose source set was evaluated (the reference probe). */
            probeId: string;
            /** @description The second probe id when the summary was evaluated over a probe pair (Phase 124); absent for the single-probe form. */
            comparedTo?: string | null;
            /** @description The resolved source set evaluated (union of both probes when paired). */
            sources?: string[];
            /** @description One entry per metric currently visible in the probe's data. */
            metrics: components["schemas"]["ProbeEquivalenceSummaryMetric"][];
        };
        /** @description One per-metric row in a `ProbeEquivalenceSummary` (Phase 115). */
        ProbeEquivalenceSummaryMetric: {
            metricName: string;
            /** @description Temporal patterns are always intra-culturally valid; this is true whenever the metric has any data in the probe scope. */
            level1Available: boolean;
            /** @description Z-score / percentile comparison is available — at least one `metric_equivalence` row at deviation-or-absolute level for this metric across the probe's languages. */
            level2Available: boolean;
            /** @description Absolute-value cross-context comparison is available — at least one `metric_equivalence` row at absolute level for this metric across the probe's languages. */
            level3Available: boolean;
            equivalenceStatus?: components["schemas"]["EquivalenceStatus"] | null;
        };
        /**
         * @description Methodological tier of the metric as defined in WP-002. Tier 1 = deterministic/lexical, Tier 2 = statistical/corpus-derived, Tier 3 = model-based/interpretive. The tier bounds the kind of claim the metric can support and is a primary signal for reflexive interpretation.
         * @example 1
         * @enum {integer}
         */
        TierClassification: 1 | 2 | 3;
        MetricProvenance: {
            /**
             * @description The canonical metric name.
             * @example sentiment_score
             */
            metricName: string;
            tierClassification: components["schemas"]["TierClassification"];
            /** @description Plain-language description of the algorithm or lexicon that produces this metric. */
            algorithmDescription: string;
            /** @description Enumerated methodological limitations documented in the relevant Working Paper. May be empty for deterministic metrics with no known limitations. */
            knownLimitations: string[];
            validationStatus: components["schemas"]["ValidationStatus"];
            /**
             * @description Short hash or version tag identifying the extractor implementation that produced the metric definition. Lets downstream consumers bind analytical results to a specific extractor revision.
             * @example v1
             */
            extractorVersionHash: string;
            /** @description Contextual notes on cross-cultural applicability, sourced from the metric_equivalence table when an entry exists. Null when no equivalence entry has been registered. */
            culturalContextNotes?: string | null;
        };
        Source: {
            /**
             * @description Canonical source identifier.
             * @example tagesschau
             */
            name: string;
            /**
             * @description Source type discriminator (e.g., "rss", "scraper").
             * @example rss
             */
            type: string;
            /** @description Origin URL (feed, API, or seed page). */
            url?: string | null;
            /**
             * @description Link to the probe dossier directory for this source (under docs/probes/<probe-id>/). The dossier groups WP-001 classification, WP-003 bias assessment, WP-005 temporal profile, and WP-006 observer-effect assessment for the probe to which this source belongs. Null when no dossier has been written.
             * @example docs/probes/probe-0-de-institutional-web/
             */
            documentationUrl?: string | null;
        };
        /**
         * @description A probe is a bundle of one or more data sources that share a methodological identity (language, institutional role, classification tier). Probes are the unit of rendering on the Atmosphere surface.
         *     This schema intentionally carries no `reach` field. A probe's reach — where its content is consumed and discursively effective — cannot be measured from emission data alone and is not claimed by AĒR. Only the emission origin(s) are asserted here.
         *     Dual-Register content (emic designation, methodological register) is served separately via `/content/probe/{probeId}` and composed on the client. Keeping structural data and editorial content on different endpoints lets the content catalog evolve (versioning, locale switching) without perturbing the typed geometry feed.
         */
        Probe: {
            /**
             * @description Canonical machine identifier, matching the dossier directory under `docs/probes/` and the `probe` content-catalog key. Stable and load-bearing (URL state, content keys); follows the convention `probe-<n>-<iso2>-<corpus-class>`. Not intended for display — use `displayName` / `shortName` in the UI.
             * @example probe-0-de-institutional-web
             */
            probeId: string;
            /**
             * @description Human-friendly probe name for UI display (globe banner, dossier headers, comparison strip). Set in the probe config; the server falls back to `probeId` when it is omitted, so this field is always present.
             * @example Germany — Institutional Web
             */
            displayName: string;
            /**
             * @description Compact label for dense UI (globe markers, comparison chips). The server falls back to `displayName` (then `probeId`) when omitted.
             * @example DE · Institutional
             */
            shortName: string;
            /**
             * @description Primary publication language as an ISO 639-1 code. Used by the client for content catalog locale fallback and for future language-scoped aggregations. Not a reach claim.
             * @example de
             */
            language: string;
            /**
             * @description Operator-declared primary country of the probe's emission origin(s), as an ISO 3166-1 alpha-2 code (e.g. `DE`, `FR`). This is an explicit, auditable classification set when the probe is registered — NOT inferred from language or coordinates. A universal facet for the Dossier search overlay (probe / source / language / country); never a reach claim.
             * @example DE
             */
            country?: string | null;
            /** @description Geographic origins of the probe's bound publishers. Each point is rendered as a glowing marker on the globe. Multiple points allow federated broadcasters or multi-publisher probes to render correctly without implying a reach region between them. */
            emissionPoints: components["schemas"]["EmissionPoint"][];
            /**
             * @description Canonical source names bound to this probe. Each entry corresponds to a row in the `sources` registry served by `/sources`. The client uses this to join per-source metrics back onto the probe marker (e.g., aggregating publication rates across `tagesschau` + `bundesregierung` into one pulse).
             * @example [
             *       "tagesschau",
             *       "bundesregierung"
             *     ]
             */
            sources: string[];
            /**
             * Format: int64
             * @description Total number of distinct processed documents across this probe's bound sources — the all-time dataset count (`count(DISTINCT article_id)` over `aer_silver.documents`), the same definition the Probe Dossier reports as `articlesTotal`. It is NOT time-window scoped: it answers "how much of the dataset does this probe contribute", independent of any analysis window. Drives the Atmosphere dataset-overview readout (Design Brief §4.1). Null when the count could not be computed (analytical store unavailable) — the probe geometry still renders; the client shows the count as unavailable rather than fabricating a zero.
             * @example 1432
             */
            documentCount?: number | null;
        };
        /**
         * @description A single geographic emission origin for a probe — the location from which one of the probe's bound sources publishes. A probe may have multiple emission points (e.g., a federated broadcaster with regional studios, or a probe bound to several institutional publishers in different cities).
         *     Emission points deliberately make no claim about a probe's *reach* — where its content is consumed, cited, or influential. Reach is not measured; inferring it from language or geography would be a fiction. At L3/L4 the methodological register must make this explicit.
         */
        EmissionPoint: {
            /**
             * Format: double
             * @description Geographic latitude of the emission origin (WGS84, degrees).
             * @example 53.5511
             */
            latitude: number;
            /**
             * Format: double
             * @description Geographic longitude of the emission origin (WGS84, degrees).
             * @example 9.9937
             */
            longitude: number;
            /**
             * @description Human-readable label for this emission point (e.g., "Hamburg (Tagesschau / NDR)"). Rendered in hover tooltips and the L3 panel.
             * @example Hamburg (Tagesschau / NDR)
             */
            label: string;
        };
        /** @description One register variant — either semantic or methodological. */
        ContentRegister: {
            /** @description Concise single-sentence summary (≤ 200 characters). Suitable for hover tooltips, badges, and compact surfaces. */
            short: string;
            /** @description Full prose explanation (≤ 2000 characters). Rendered in Layer 4 (Provenance) panels where the user has descended to methodological detail. */
            long: string;
        };
        /** @description The paired semantic and methodological registers for one entity. Both registers are always present; the frontend decides which is prominent. */
        ContentRegisters: {
            semantic: components["schemas"]["ContentRegister"];
            methodological: components["schemas"]["ContentRegister"];
        };
        /** @description Dual-Register content record for one entity. Sourced from versioned YAML files under configs/content/. Both registers are guaranteed present when the record is returned. */
        ContentResponse: {
            /**
             * @description Canonical identifier of the entity.
             * @example sentiment_score
             */
            entityId: string;
            /**
             * @description Category of the entity.
             * @example metric
             * @enum {string}
             */
            entityType: "metric" | "probe" | "source" | "discourse_function" | "refusal" | "view_mode" | "empty_lane" | "open_research_question" | "primer";
            /**
             * @description Language of the returned content.
             * @example en
             * @enum {string}
             */
            locale: "en" | "de";
            registers: components["schemas"]["ContentRegisters"];
            /**
             * @description Semver-style version string for the content record, e.g. "v2026-04-a". Incremented on every editorial update so consumers can detect staleness.
             * @example v2026-04-a
             */
            contentVersion: string;
            /**
             * @description Name or role of the last reviewer.
             * @example Engineering Team
             */
            lastReviewedBy: string;
            /**
             * Format: date
             * @description ISO 8601 date of last review.
             * @example 2026-04-19
             */
            lastReviewedDate: string;
            /**
             * @description Optional list of Working Paper section references (e.g., "WP-002 §3") for downstream citation linking.
             * @example [
             *       "WP-002 §3"
             *     ]
             */
            workingPaperAnchors?: string[] | null;
        };
        /**
         * @description Composite payload backing Surface II's Probe Dossier (Design Brief §4.2.1 in the Iteration 5 rewrite). The dossier answers a serious user's first question — *what is actually in this probe?* — by combining the probe's structural identity with the per-source counts and frequencies needed to drive the source cards on the Dossier landing.
         *     The payload is composed server-side rather than fetched as separate resources because the Dossier is the canonical Surface II landing and the client must avoid request waterfalls on first paint.
         */
        ProbeDossier: {
            /**
             * @description Canonical machine probe identifier (matches `/probes` and `/content/probe/{probeId}`). Not for display — use `displayName` / `shortName`.
             * @example probe-0-de-institutional-web
             */
            probeId: string;
            /**
             * @description Human-friendly probe name for UI display; server falls back to `probeId` when unset.
             * @example Germany — Institutional Web
             */
            displayName: string;
            /**
             * @description Compact probe label; server falls back to `displayName` (then `probeId`).
             * @example DE · Institutional
             */
            shortName: string;
            /**
             * @description Primary publication language (ISO 639-1).
             * @example de
             */
            language: string;
            /**
             * Format: date-time
             * @description Start of the window over which per-source `articlesInWindow` counts were computed. Null when no window was supplied.
             */
            windowStart?: string | null;
            /**
             * Format: date-time
             * @description End of the article-count window. Null when no window was supplied.
             */
            windowEnd?: string | null;
            /** @description Probe-level WP-001 §5.1 function coverage — how many of the four discourse functions the probe's sources collectively address. A derived property; not a constraint on the probe. */
            functionCoverage: {
                /**
                 * @description Number of distinct primary functions present.
                 * @example 2
                 */
                covered: number;
                /**
                 * @description Total catalog size (always 4 — the WP-001 functions).
                 * @example 4
                 */
                total: number;
                /** @description The distinct primary functions present in this probe. */
                functions: ("epistemic_authority" | "power_legitimation" | "cohesion_identity" | "subversion_friction")[];
            };
            /** @description Phase 123a — per-probe analytical capability matrix. Describes what AĒR CAN compute for this probe; it asserts no results and is never a cross-probe ranking signal (WP-006 discovery-bias guard). Drives the Dossier capability readout. */
            capabilities?: {
                /**
                 * @description The cross-language sentiment backbone metric for the probe's language (from the Language Capability Manifest). Null when the language has no sentiment capability.
                 * @example sentiment_score_bert_multilingual
                 */
                sentimentBackbone?: string | null;
                /**
                 * @description Optional per-language sentiment refinements layered on the backbone.
                 * @example [
                 *       "sentiment_score_bert_de_news"
                 *     ]
                 */
                sentimentEnrichments: string[];
                /** @description Whether silent-edit / revision observability (Phase 122d Wayback CDX sidecar) is active for the probe's sources. */
                silentEditObservability: boolean;
                /**
                 * @description Per-article discourse-function classifier status. Currently always "deferred / source-level only" (Phase 122a deferred — see ADR-030).
                 * @example deferred / source-level only
                 */
                discourseFunctionClassifier: string;
            };
            /** @description One card per source in the probe, ordered by source name. */
            sources: components["schemas"]["ProbeDossierSource"][];
        };
        /** @description Per-source card on the Probe Dossier. Combines the structural source row with the etic/emic classification (WP-001), the Silver-eligibility flag (Phase 101 §3.4), and the article counts that drive the Dossier UI. */
        ProbeDossierSource: {
            /**
             * @description Canonical source name.
             * @example tagesschau
             */
            name: string;
            /**
             * @description Source type discriminator (e.g., "rss").
             * @example rss
             */
            type: string;
            /** @description Origin URL. */
            url?: string | null;
            /** @description Probe dossier directory under `docs/probes/`. */
            documentationUrl?: string | null;
            /** @description Total number of processed documents for this source. */
            articlesTotal: number;
            /** @description Number of processed documents falling in the requested window. Equal to `articlesTotal` when no window was supplied. */
            articlesInWindow: number;
            /** @description Phase 122d.2 — number of this source's articles (window-scoped) whose timestamp is the crawler fetch time, not a real publication date (`timestamp_source='fetch_at_fallback'`) — the Temporal-Provenance-Absence Negative-Space class. A reflexive disclosure about the SOURCE ("X of N articles have no real publication date"), never a defect. Null when no window context applies. */
            temporalProvenanceAbsentCount?: number | null;
            /** @description Average publications per day across the requested window (or full history when no window is supplied). Null when the source has no processed documents yet. */
            publicationFrequencyPerDay?: number | null;
            /**
             * @description WP-001 etic primary discourse function from the latest classification record. Null when the source has no classification.
             * @enum {string|null}
             */
            primaryFunction?: "epistemic_authority" | "power_legitimation" | "cohesion_identity" | "subversion_friction" | null;
            /**
             * @description WP-001 etic secondary discourse function. Null when none.
             * @enum {string|null}
             */
            secondaryFunction?: "epistemic_authority" | "power_legitimation" | "cohesion_identity" | "subversion_friction" | null;
            /** @description WP-001 emic designation — how the source describes itself. */
            emicDesignation?: string | null;
            /** @description WP-001 emic context — the cultural/discursive frame of the source. */
            emicContext?: string | null;
            /** @description Whether the source is approved for Silver-layer self-service access per WP-006 §5.2. Probe 0's two sources are auto-eligible; all others default to false until reviewed. */
            silverEligible: boolean;
            /**
             * Format: date
             * @description Date the Silver-access review was completed (null when not reviewed).
             */
            silverReviewDate?: string | null;
        };
        /** @description Light-weight article reference returned by the article-browsing endpoint. Carries enough metadata for a list view without leaking cleaned text. Cleaned text and full provenance are served from `/articles/{id}` and are subject to the k-anonymity gate (WP-006 §7). */
        ArticleListItem: {
            /**
             * @description SHA-256 hash of source + bronze_object_key (worker-derived).
             * @example 7c1c1f...
             */
            articleId: string;
            /** @description Canonical source name. */
            source: string;
            /**
             * Format: date-time
             * @description Publication timestamp from SilverCore.
             */
            timestamp: string;
            /** @description Phase 122d.2 — provenance of `timestamp`. `fetch_at_fallback` means the timestamp is the crawler fetch time, not a real publication date (the Temporal-Provenance-Absence Negative-Space class — WP-005 §3.1: "a publication gap is an observation gap, not a discourse gap"). Other values (`json_ld_published`, `open_graph_published`, …) are real dates; absent/ empty = a legacy/non-web row predating this provenance dimension. */
            timestampSource?: string | null;
            /** @description Top language detection result for the article (ISO 639-1). */
            language?: string | null;
            /** @description Word count from `aer_gold.metrics`. */
            wordCount?: number | null;
            /** @description Sentiment score from `aer_gold.metrics` (provisional, WP-002). */
            sentimentScore?: number | null;
            /** @description Number of archived CAPTURES detected for this article (Wayback CDX snapshots + republication triggers) — a coverage/tooling count, NOT a revision count (most captures are identical re-archivals). Only present when the request opts in via `?includeRevisions=true`. For "how often the article was actually edited", use `editorialChangeCount`. */
            chainLength?: number | null;
            /** @description Phase 133 — number of CONFIRMED editorial changes for this article (content/headline changed between consecutive observed states, incl. newest-snapshot → current). This is the only count surfaced as "revisions". 0 = observed but never edited. Only present when the request opts in via `?includeRevisions=true`. */
            editorialChangeCount?: number | null;
            /** @description Phase 122d.1 — true iff at least one revision in this article's chain reports `headline_changed=true`. Only present when the request opts in via `?includeRevisions=true`. */
            hasHeadlineChange?: boolean | null;
            /**
             * Format: date-time
             * @description Phase 122d.1 — timestamp of the most recent revision in this article's chain. Only present when the request opts in via `?includeRevisions=true`.
             */
            latestRevisionAt?: string | null;
        };
        /** @description Paginated article-listing response. Cursor-based to keep deep paging stable under late-arriving Gold rows; `nextCursor` is opaque and must be echoed back unchanged on the next request. */
        ArticlesPage: {
            items: components["schemas"]["ArticleListItem"][];
            /** @description True when more results exist beyond this page. */
            hasMore: boolean;
            /** @description Opaque pagination token. Pass back via `?cursor=` to fetch the next page. Null when `hasMore` is false. */
            nextCursor?: string | null;
        };
        /** @description Full article payload for L5 Evidence (Design Brief §4.5.5 in the Iteration 5 rewrite). Includes Bronze cleaned text, Silver metadata, and per-extractor provenance. Subject to a k-anonymity gate (WP-006 §7): if the article's aggregation group for the referenced metric is below the configured threshold, the BFF returns 403 with a methodological refusal payload instead of the article body. */
        ArticleDetail: {
            articleId: string;
            source: string;
            sourceType?: string;
            /** Format: date-time */
            timestamp: string;
            url?: string | null;
            /** @description Cleaned text from SilverCore — the text the extractors operated on. */
            cleanedText: string;
            /** @description Raw text as fetched from the source. May be null when the harmoniser did not preserve it (older Silver schema versions). */
            rawText?: string | null;
            language?: string | null;
            /** @description SilverCore schema version present at extraction time. */
            schemaVersion: string;
            wordCount: number;
            /** @description Source-specific SilverMeta payload (e.g., RSS feed_url, categories). */
            meta?: {
                [key: string]: unknown;
            } | null;
            /** @description Per-extractor version hashes captured at extraction time. */
            extractionProvenance?: {
                [key: string]: string;
            };
        };
        /** @description Methodological refusal payload returned when the BFF declines to serve a resource because a methodological gate (k-anonymity, equivalence, Silver-eligibility) is not satisfied. The shape is intentionally separate from the generic Error schema so the frontend can render the refusal as a Surface III-linked methodological surface rather than a bare error toast (Brief §3.3). */
        RefusalPayload: {
            /**
             * @description Machine identifier of the gate that fired. The `metric_equivalence` value (Phase 115) is returned when a cross-frame normalization request lacks a deviation-level entry in `aer_gold.metric_equivalence`. The `invalid_language` value (Phase 118a / ADR-024) is returned when a `?language=` query parameter is not declared in the Language Capability Manifest; `alternatives` then carries the manifest's sorted language codes.
             * @example k_anonymity
             * @enum {string}
             */
            gate: "k_anonymity" | "silver_eligibility" | "equivalence" | "metric_equivalence" | "invalid_language";
            /** @description Concrete user-actionable alternatives the dashboard can offer when the gate refuses (Phase 115). Each entry is a short imperative describing what the user can do to obtain a comparable view — e.g. "drop normalization to Level 1 (temporal patterns only)", "constrain scope to one cultural frame", "use deviation labelling instead of an absolute claim". */
            alternatives?: string[] | null;
            /** @description Human-readable summary of the refusal. */
            message: string;
            /** @description Threshold value the gate enforces (e.g., minimum aggregation size for k-anonymity). */
            threshold?: number | null;
            /** @description Observed value that fell below the threshold. */
            observed?: number | null;
            /** @description Anchor into the methodological surface (e.g., `WP-006#section-7`) that explains the gate. */
            workingPaperAnchor?: string | null;
        };
        /** @description Per-scope distribution of a metric over a time window. Includes both histogram bins and a quantile summary so the frontend can render histogram, density, ridgeline, or violin without a second round-trip. */
        DistributionResponse: {
            /** @description The metric name that was queried. */
            metricName: string;
            /** @description The resolved scope (`probe` or `source`). */
            scope?: string;
            /** @description The resolved scope identifier. */
            scopeId?: string;
            /**
             * Format: date-time
             * @description Resolved lower window bound; null when the query was unbounded (whole dataset).
             */
            windowStart?: string | null;
            /**
             * Format: date-time
             * @description Resolved upper window bound; null when unbounded.
             */
            windowEnd?: string | null;
            /**
             * Format: double
             * @description Upper bound of the histogram bin domain. Equals `summary.max` when no outliers were detected; otherwise the Tukey upper fence (p75 + 1.5·IQR), with values above it diverted into `overflowCount` so a single extreme value cannot collapse the visible distribution into one bar (Phase 133 B). The `summary` still reports the true max.
             */
            clampedUpper: number;
            /**
             * Format: int64
             * @description Number of articles whose value exceeds `clampedUpper`. Zero when the distribution was not clamped. Disclosed so the robust clamp is never a silent truncation.
             */
            overflowCount: number;
            /** @description Histogram bins ordered ascending by `lower`, spanning `[summary.min, clampedUpper]`. Values above `clampedUpper` are NOT in these bins — see `overflowCount`. */
            bins: {
                /**
                 * Format: double
                 * @description Inclusive lower edge of the bin.
                 */
                lower: number;
                /**
                 * Format: double
                 * @description Exclusive upper edge of the bin (inclusive on the last bin).
                 */
                upper: number;
                /** Format: int64 */
                count: number;
            }[];
            /** @description Quantile and basic statistics for the same window. */
            summary: {
                /** Format: int64 */
                count: number;
                /** Format: double */
                min: number;
                /** Format: double */
                max: number;
                /** Format: double */
                mean: number;
                /** Format: double */
                median: number;
                /** Format: double */
                p05: number;
                /** Format: double */
                p25: number;
                /** Format: double */
                p75: number;
                /** Format: double */
                p95: number;
            };
            /** @description Per-segment payloads populated when `segmentBy` is set. Absent when `segmentBy` is not requested. The aggregate `bins` and `summary` are always present and reflect the full union scope (Phase 114). */
            streams?: {
                /** @description Segment identifier (source name or probe id). */
                id: string;
                /** @description Display label for the segment. */
                label: string;
                /** @description Segment kind: `source` or `probe`. */
                scopeKind: string;
                /** @description Histogram bins ordered ascending by `lower`. */
                bins: {
                    /** Format: double */
                    lower: number;
                    /** Format: double */
                    upper: number;
                    /** Format: int64 */
                    count: number;
                }[];
                summary: {
                    /** Format: int64 */
                    count: number;
                    /** Format: double */
                    min: number;
                    /** Format: double */
                    max: number;
                    /** Format: double */
                    mean: number;
                    /** Format: double */
                    median: number;
                    /** Format: double */
                    p05: number;
                    /** Format: double */
                    p25: number;
                    /** Format: double */
                    p75: number;
                    /** Format: double */
                    p95: number;
                };
            }[];
        };
        /** @description Top-N categorical distribution of one metadata field over a scope (Phase 133). Counts are DISTINCT in-scope articles per value; the long tail beyond the Top-N is disclosed so the truncation is never silent. */
        CategoricalDistributionResponse: {
            /**
             * @description The categorical metadata field this distribution is over.
             * @example section
             */
            field: string;
            /** @description Resolved scope kind ("probe" or "source"). */
            scope?: string;
            /** @description Resolved scope identifier, when a single scopeId was supplied. */
            scopeId?: string;
            /** Format: date-time */
            windowStart?: string;
            /** Format: date-time */
            windowEnd?: string;
            /** @description The top-N values, ranked by distinct-article count (desc, then value asc). */
            categories: {
                /** @description One categorical value (e.g. a section name, a tag, an author). */
                value: string;
                /** @description Number of distinct in-scope articles carrying this value. */
                articles: number;
            }[];
            /** @description Distinct in-scope articles that carry ANY value for this field. */
            totalArticles: number;
            /** @description Total number of distinct values for this field in scope. When greater than the length of `categories`, the client discloses "showing top N of M". */
            distinctValues: number;
            /** @description Summed per-value article weight beyond the Top-N (for an optional "other" bar). For list fields one article can carry several values, so this is a value-occurrence weight, NOT a distinct-article count — use `distinctValues` for the unambiguous category count. */
            otherArticles: number;
        };
        /** @description Per-scope categorical-metadata availability (Phase 133) — the categorical analog of ScopeAvailableMetrics. Drives the dimension picker's metadata group so a cross-probe panel never binds a field absent on some scoped source. */
        ScopeAvailableMetadata: {
            /** @description The resolved source names the availability was computed over. */
            scopedSources: string[];
            /** @description Categorical metadata fields that have data for EVERY scoped source in the window. The only fields safe to bind on a panel spanning the whole scope. */
            available: string[];
            /** @description Fields present for SOME but not all scoped sources, with the subset of sources that carry them. Surfaced as an explanatory hint, never offered for binding (unless the user opts in). */
            partial: {
                field: string;
                /** @description The scoped sources that have data for this field. */
                sources: string[];
            }[];
            /** @description Categorical fields whose value is CONSTANT across the whole scope (exactly one distinct value) — present, but carrying no signal: grouping/cross-tab by a constant field is meaningless. The dashboard drops these from the dimension picker, but they are disclosed here with the constant value so the omission is never silent (ADR-039 DISCLOSE-NEVER-COERCE). Self- maintaining: a future source that introduces real variance removes the field from this list automatically. */
            degenerate?: {
                field: string;
                /** @description The single constant value observed across the scope. */
                value: string;
            }[];
            /** @description Available fields with ≥2 distinct values but a strongly DOMINANT value (near-constant: dominantShare ≥ the disclosed display threshold). Unlike `degenerate` these are NOT dropped — a rare-but-real minority category must stay reachable — only DISCLOSED so a cell/picker can flag "effectively constant — NN% <value>". This carries the raw concentration measurement; the backend only measures, it does not drop. The threshold that decides membership is an engineering-default *display* cutoff (provisional), not a methodological constant. */
            lowSignal?: {
                field: string;
                /** @description Number of distinct values for the field across the scope. */
                distinctValues: number;
                /** @description Fraction of in-scope articles carrying the single most common value (0..1). A value near 1 means the field is effectively constant. */
                dominantShare: number;
                /** @description The single most common value for the field across the scope. */
                dominantValue: string;
            }[];
            /** Format: date-time */
            windowStart?: string;
            /** Format: date-time */
            windowEnd?: string;
        };
        /** @description Two-dimensional binning of a metric over a time window. `xDimension` and `yDimension` are echoed back for the frontend's axis labelling. */
        HeatmapResponse: {
            metricName: string;
            xDimension: string;
            yDimension: string;
            scope?: string;
            scopeId?: string;
            /**
             * Format: date-time
             * @description Resolved lower window bound; null when the query was unbounded (whole dataset).
             */
            windowStart?: string | null;
            /**
             * Format: date-time
             * @description Resolved upper window bound; null when unbounded.
             */
            windowEnd?: string | null;
            /** @description One row per non-empty (x, y) cell. */
            cells: {
                /** @description The x-axis bucket label (string-encoded so int / category fit one shape). */
                x: string;
                /** @description The y-axis bucket label. */
                y: string;
                /**
                 * Format: double
                 * @description Mean metric value across rows in this cell.
                 */
                value: number;
                /**
                 * Format: int64
                 * @description Number of source rows aggregated into this cell.
                 */
                count: number;
            }[];
            /** @description Per-segment payloads populated when `segmentBy` is set. Absent when `segmentBy` is not requested. The aggregate `cells` are always present and reflect the full union scope (Phase 114). */
            streams?: {
                /** @description Segment identifier (source name or probe id). */
                id: string;
                /** @description Display label for the segment. */
                label: string;
                /** @description Segment kind: `source` or `probe`. */
                scopeKind: string;
                /** @description One row per non-empty (x, y) cell for this segment. */
                cells: {
                    x: string;
                    y: string;
                    /** Format: double */
                    value: number;
                    /** Format: int64 */
                    count: number;
                }[];
            }[];
        };
        /** @description Pairwise Pearson correlation matrix over the requested metrics, computed from per-bucket means within the window. `matrix[i][j]` is the correlation of `metrics[i]` and `metrics[j]`. The diagonal is 1.0 when defined; cells with insufficient overlapping samples are returned as `null`. */
        CorrelationMatrix: {
            metrics: string[];
            /** @description NxN matrix of correlations; outer index matches `metrics`. */
            matrix: (number | null)[][];
            /**
             * Format: int64
             * @description Number of time buckets used to compute the correlation.
             */
            bucketCount: number;
            /** @description Bucket resolution used (e.g. `5m`). */
            resolution: string;
            scope?: string;
            scopeId?: string;
            /**
             * Format: date-time
             * @description Resolved lower window bound; null when the query was unbounded (whole dataset).
             */
            windowStart?: string | null;
            /**
             * Format: date-time
             * @description Resolved upper window bound; null when unbounded.
             */
            windowEnd?: string | null;
        };
        /** @description Aggregated entity-pair co-occurrence graph for the Network Science x force-directed-graph view mode. Edges are the top-N pairs by summed cooccurrence_count over the window; nodes are the union of incident entities, sized by their per-window total mention count. */
        CoOccurrenceGraph: {
            scope?: string;
            scopeId?: string;
            /**
             * Format: date-time
             * @description Resolved lower window bound; null when the query was unbounded (whole dataset).
             */
            windowStart?: string | null;
            /**
             * Format: date-time
             * @description Resolved upper window bound; null when unbounded.
             */
            windowEnd?: string | null;
            /**
             * Format: int64
             * @description The effective top-N applied (post-clamp).
             */
            topN: number;
            nodes: {
                text: string;
                label: string;
                /**
                 * Format: int64
                 * @description Number of distinct neighbours in the returned edge set.
                 */
                degree: number;
                /**
                 * Format: int64
                 * @description Sum of edge weights incident on this node.
                 */
                totalCount: number;
                /** @description Source names where this entity appears within the returned edge set and window. Populated when the scope covers multiple sources, so the frontend can render per-source incident shading without a follow-up call (Phase 114). */
                presence?: string[];
                /** @description Canonical Wikidata QID resolved by the Phase 118 entity-linking step, or null when the node could not be linked. Lets the frontend surface Wikipedia/Wikidata external links on graph nodes without a follow-up call. */
                wikidataQid?: string | null;
                /** @description Phase 123b cross-lingual relabel. The QID's display label in the requested `viewerLanguage`, or null when no viewer language was requested, the node has no QID, or no label exists for that language. The frontend relabels a node only when this is present; otherwise it keeps the source surface form (`text`). This is the per-language rdfs:label Wikidata publishes — never a machine translation. */
                viewerLabel?: string | null;
                /**
                 * Format: double
                 * @description Phase 125 node-metric binding. The mean of the requested `nodeMetric` over the articles where this entity appears, or null when no `nodeMetric` was requested or no article carrying it mentions the entity. Lets the network cell size/colour nodes by a metric (e.g. mean sentiment of the mentioning articles) rather than only graph-intrinsic degree/weight. This is the SIZE-channel metric when size and colour bind to different metrics.
                 */
                metricValue?: number | null;
                /**
                 * Format: double
                 * @description Phase 125 / ISSUE 7 — the mean of the requested `nodeColorMetric` over this entity's articles, present only when a separate colour metric was requested AND it differs from `nodeMetric`. null otherwise (then the colour channel reuses `metricValue`). Lets the colour channel bind to a different metric than the size channel.
                 */
                metricValueColor?: number | null;
            }[];
            edges: {
                /** @description Lexicographically smaller entity text in the pair. */
                a: string;
                b: string;
                aLabel?: string;
                bLabel?: string;
                /**
                 * Format: int64
                 * @description Sum of cooccurrence_count over articles in the window.
                 */
                weight: number;
                /**
                 * Format: int64
                 * @description Number of distinct articles contributing to this edge.
                 */
                articleCount: number;
                /**
                 * Format: int64
                 * @description Phase 122d.2 — number of this edge's contributing articles with NO real publication date (`timestamp_source='fetch_at_fallback'`), populated only when `?negativeSpaceOverlay=ghost`. A reflexive disclosure that the connection leans on undated articles (WP-005 §3.1); the edge is NOT filtered. 0 / omitted otherwise.
                 */
                nsSupport?: number;
                /** @description Source names this edge was observed in within the returned window (Phase 131a). Lets the frontend render a source-coloured overlay on a merged multi-source graph without a follow-up call. Populated whenever the scope covers multiple sources; omitted (or a single entry) for single-source scopes. */
                presence?: string[];
            }[];
            /**
             * Format: int64
             * @description Count of distinct articles in the window whose `aer_gold.entities` contain ≥2 entities for the resolved scope. The Phase 131a pipeline-gap diagnostic: when `articlesInScope > 0` but `edges` is empty, the dashboard surfaces a "pipeline gap" hint instead of "sparse corpus" — distinguishing missing data from a missing sweep. Always populated.
             */
            articlesInScope?: number;
            /**
             * Format: int64
             * @description Phase 123b — how many returned nodes carry a Wikidata QID (the subset the cross-lingual relabel toggle can act on). The dashboard shows the linked-vs-unlinked coverage ratio against `len(nodes)` so a reader knows how much of a foreign-language graph the toggle can relabel (WP-006 no-silent-gaps). Always populated.
             */
            linkedNodeCount?: number;
            /**
             * Format: int64
             * @description Phase 123b — how many nodes received a `viewerLabel` in the requested `viewerLanguage` (always ≤ `linkedNodeCount`; a linked QID may lack a label in that language). Zero when no viewer language was requested.
             */
            labeledNodeCount?: number;
        };
        /** @description A single source record carrying the fields from the Source schema plus the Silver-layer eligibility state and the WP-006 §5.2 review metadata recorded at the time `silverEligible` was flipped. Fields beyond `silverEligible` are present only on eligible sources (and may be empty on auto-eligible Probe 0 sources where the metadata is a stub). */
        SourceDetail: {
            /** @description Canonical source identifier. */
            name: string;
            /** @description Source type discriminator (e.g. `rss`, `scraper`). */
            type: string;
            url?: string | null;
            documentationUrl?: string | null;
            /** @description Whether this source has passed the WP-006 §5.2 review for Silver-layer self-service access. */
            silverEligible: boolean;
            /** @description Reviewer of record (free text). */
            silverReviewReviewer?: string | null;
            /** Format: date */
            silverReviewDate?: string | null;
            /** @description Free-text justification for the eligibility decision. */
            silverReviewRationale?: string | null;
            /** @description Pointer to the canonical reference document for this review (e.g. `docs/arc42/09_architecture_decisions.md#adr-020`). */
            silverReviewReference?: string | null;
        };
        /** @description Compact summary of a Silver-layer document for the paginated list endpoint. The list response is composed from PostgreSQL (`documents` → `ingestion_jobs` → `sources`) joined with ClickHouse Gold-layer rows (word_count metric, rank-1 language detection) so a single page does not require a per-row MinIO read. Use the detail endpoint to retrieve the full SilverEnvelope with `cleanedText`, `rawText`, `meta`, and per-extractor provenance. */
        SilverDocumentSummary: {
            articleId: string;
            source: string;
            /** Format: date-time */
            timestamp: string;
            language?: string | null;
            wordCount?: number | null;
        };
        /** @description Paginated page of Silver-layer documents for a single source. The cursor is opaque (base64 token over an offset today; may evolve without an API break). */
        SilverDocumentsPage: {
            /** @description The source name the page is scoped to. */
            source: string;
            items: components["schemas"]["SilverDocumentSummary"][];
            hasMore: boolean;
            nextCursor?: string | null;
        };
        /** @description Full Silver-layer document detail. Returns the SilverCore record (cleaned_text included) plus the SilverMeta blob (source-specific, intentionally unstable per ADR-015) and the per-extractor provenance recorded at extraction time. */
        SilverDocumentDetail: {
            articleId: string;
            source: string;
            sourceType?: string | null;
            /** Format: date-time */
            timestamp: string;
            schemaVersion: string;
            language?: string | null;
            url?: string | null;
            rawText?: string | null;
            cleanedText: string;
            wordCount: number;
            meta?: {
                [key: string]: unknown;
            } | null;
            extractionProvenance?: {
                [key: string]: string;
            } | null;
        };
        /** @description Result of a Silver aggregation query. Exactly one of `distribution`, `heatmap`, or `correlation` is populated, matching the requested `aggregationType`. The frontend dispatches on `aggregationType` rather than introspecting which payload field is present. */
        SilverAggregationResponse: {
            /** @description Echoes the requested aggregation type. */
            aggregationType: string;
            /** @description Resolved canonical source name. */
            source: string;
            /** Format: date-time */
            windowStart: string;
            /** Format: date-time */
            windowEnd: string;
            /** @description Populated for `cleaned_text_length`, `word_count`, `raw_entity_count`. */
            distribution?: {
                bins: {
                    /** Format: double */
                    lower: number;
                    /** Format: double */
                    upper: number;
                    /** Format: int64 */
                    count: number;
                }[];
                summary: {
                    /** Format: int64 */
                    count: number;
                    /** Format: double */
                    min: number;
                    /** Format: double */
                    max: number;
                    /** Format: double */
                    mean: number;
                    /** Format: double */
                    median: number;
                    /** Format: double */
                    p05: number;
                    /** Format: double */
                    p25: number;
                    /** Format: double */
                    p75: number;
                    /** Format: double */
                    p95: number;
                };
            };
            /** @description Populated for `cleaned_text_length_by_hour`, `word_count_by_source`. */
            heatmap?: {
                xDimension: string;
                yDimension: string;
                cells: {
                    x: string;
                    y: string;
                    /**
                     * Format: double
                     * @description Mean of the projection field across rows in this cell.
                     */
                    value: number;
                    /** Format: int64 */
                    count: number;
                }[];
            };
            /** @description Populated for `cleaned_text_length_vs_word_count`. */
            correlation?: {
                /** @description Projection-field names whose pairwise correlation was computed. */
                fields: string[];
                /** @description NxN Pearson correlation matrix; outer index matches `fields`. Cells with insufficient samples are `null`. */
                matrix: (number | null)[][];
                /**
                 * Format: int64
                 * @description Number of documents that contributed to the correlation.
                 */
                sampleCount: number;
            };
        };
        /** @description Per-language topic distribution payload backing the Episteme-pillar `topic_distribution` and `topic_evolution` view modes. Topics are fit per language by BERTopic (Phase 120 / WP-004 §3.4); the `language` field on each entry is mandatory and `topic_id` is unique only within one language partition. Cross-language topic comparisons are meaningless and must not be performed at the rendering layer. */
        TopicDistributionResponse: {
            scope?: string;
            scopeId?: string;
            /** Format: date-time */
            windowStart: string;
            /** Format: date-time */
            windowEnd: string;
            /** @description The language filter actually applied. Empty when no `language` parameter was supplied — in that case the `topics` array contains one entry per (language, topic_id) pair, sorted by `articleCount` across all languages. */
            language?: string;
            /** @description Topic entries sorted by `articleCount` descending. Capped at 50. The outlier topic (`topicId == -1`) is omitted unless the request sets `includeOutlier=true`; when present, its `label` is "uncategorised" per the Phase 121 frontend convention. */
            topics: {
                /**
                 * Format: int32
                 * @description BERTopic numeric identifier; `-1` is the outlier class.
                 */
                topicId: number;
                /** @description BERTopic c-TF-IDF / KeyBERT representation, or "uncategorised" for outliers. */
                label: string;
                /**
                 * Format: int64
                 * @description Distinct articles assigned to this topic in the window.
                 */
                articleCount: number;
                /**
                 * Format: float
                 * @description Mean `topic_confidence` over the included rows.
                 */
                avgConfidence: number;
                /** @description ISO-639-1 language code of the language partition. */
                language: string;
                /** @description BERTopic model-provenance hash (Phase 120). When the topic was assigned by more than one model in the window, the most-recent hash is reported — clients should treat a non-unique value as a signal that topic identity has rotated within the window. */
                modelHash?: string;
            }[];
        };
        /** @description Per-field metadata coverage for a single source. Backs Phase 122f's field-level Negative-Space rendering (Design Brief §7.7) and the Probe Dossier metadata-coverage panel. WP-003 §3.2 frames why structural absence must be surfaced as the publisher's emission posture rather than as missing data. */
        MetadataCoverageField: {
            /**
             * @description Tier-B / Tier-C field name as recorded by the WebAdapter's `extraction_methods` provenance dict (e.g. `published_date`, `author`, `articleSection`).
             * @example author
             */
            field: string;
            /**
             * @description Distinct article count observed for this (source, field) over the 30-day metadata-coverage horizon (TTL on the raw fact table).
             * @example 120
             */
            totalArticles: number;
            /**
             * @description Per-extraction-method article counts. Keys are the method values from `web_meta.ALLOWED_EXTRACTION_METHODS` plus the literal string `"null"` for unfilled fields. The literal-string `"null"` is the Negative-Space sentinel — structural absence as a queryable value, not a SQL NULL.
             * @example {
             *       "json_ld": 49,
             *       "heuristic_htmldate": 8,
             *       "null": 63
             *     }
             */
            byMethod: {
                [key: string]: number;
            };
            /**
             * Format: double
             * @description Fraction of articles where this field was populated by any non-null method. `1 - byMethod["null"] / totalArticles`.
             * @example 0.475
             */
            populationRate: number;
            /**
             * @description True when 0 % of ≥ 50 observed articles populated this field over the last 30 days. Encodes "the absence is the publisher's choice, not sampling variance" — drives the dashboard's field-level Negative-Space rendering (Brief §7.7).
             * @example true
             */
            structurallyAbsent: boolean;
            /**
             * @description Task A — true when the field is PRESENT but carries exactly one distinct value across this source's corpus (zero variance). Such a field is dropped from the Workbench picker (a distribution / grouping over a constant is meaningless), so the dossier marks it here to explain why a 100 %-populated field is not offered for analysis — "present but constant → no signal", never silently hidden (ADR-039). Computed BFF-side over `aer_gold.article_metadata` (categorical) / `aer_gold.metrics` (scalar metadata), not from the coverage MV, so no worker change is needed. Self-maintaining: real future variance clears the flag automatically.
             * @example true
             */
            constant?: boolean;
            /**
             * @description The single constant value, present only when `constant` is true. A categorical field carries its string value; a scalar-metadata field carries its numeric value rendered as a string (e.g. "1", "0").
             * @example NewsArticle
             */
            constantValue?: string;
        };
        /** @description Per-source metadata coverage record carrying the per-field matrix for one source. Sources with no observed articles in the metadata-coverage horizon return an empty `fields` array — same shape, semantically "no observations yet". */
        MetadataCoverageSource: {
            /**
             * @description Canonical source name (matches `Source.name`).
             * @example bundesregierung
             */
            name: string;
            /** @description One entry per Tier-B / Tier-C field observed for this source. */
            fields: components["schemas"]["MetadataCoverageField"][];
        };
        /**
         * @description Composite payload for the Phase 122f metadata-coverage endpoints. Returned by both `GET /probes/{probeId}/metadata-coverage` (multi-source view backing the Probe Dossier panel) and `GET /sources/{sourceId}/metadata-coverage` (single-source view backing the per-source dossier surface). The shape is the same — only the scope of the `sources` array differs.
         *     WP-003 §3.2 documents the metadata-richness asymmetry as a structural bias; this endpoint operationalises it. AĒR analysis layers MUST consume the signal for any cross-source aggregation — absent fields are absent by design, not by sampling variance.
         */
        MetadataCoverageResponse: {
            /**
             * @description Either the probeId (when called via `/probes/{probeId}/metadata-coverage`) or the sourceId (when called via `/sources/{sourceId}/metadata-coverage`).
             * @example probe-0-de-institutional-web
             */
            scope: string;
            /** @description One entry per source in the requested scope, ordered by source name. Empty when the scope resolves to no sources. */
            sources: components["schemas"]["MetadataCoverageSource"][];
        };
        /** @description Corpus-wide extraction + variance status for one metadata field (Task C). Every figure is measured over the WHOLE observed corpus (all sources), so the Reflection page states the real fill rate rather than a per-probe slice. */
        MetadataFieldStat: {
            /**
             * @description The Tier-B / Tier-C field name (matches the coverage matrix).
             * @example article_type
             */
            field: string;
            /**
             * @description Distinct articles for which this field was observed across all sources.
             * @example 4200
             */
            totalArticles: number;
            /**
             * @description Distinct articles where the field was filled by a real (non-null) extraction method. The complement is structural absence — the publisher did not declare the field, NOT an AĒR extraction defect.
             * @example 4200
             */
            populatedArticles: number;
            /**
             * Format: double
             * @description populatedArticles / totalArticles (0 when never observed).
             * @example 1
             */
            populationRate: number;
            /**
             * @description Number of sources that emitted any article carrying this field column.
             * @example 4
             */
            sourcesObserved: number;
            /**
             * @description Number of sources where the field is populated for at least one article.
             * @example 2
             */
            sourcesPopulated: number;
            /**
             * @description Distinct values observed for this field across the whole corpus (0 when never populated). 1 means the field is constant everywhere — present but signal-free (the Task-A "no signal" condition at corpus scale).
             * @example 1
             */
            distinctValues: number;
            /**
             * @description True when the field is populated but carries exactly one distinct value across the entire corpus (distinctValues == 1) — present but signal-free.
             * @example true
             */
            constant: boolean;
            /**
             * @description The single constant value, present only when `constant` is true.
             * @example NewsArticle
             */
            constantValue?: string;
        };
        /** @description Corpus-wide extraction status for AĒR's Tier-B / Tier-C metadata fields (Task C — the Reflection "metadata fields" surface). Unlike the per-probe metadata-coverage matrix, this aggregates over EVERY observed source so the Reflection page can answer, honestly and live, "which structured-metadata fields does AĒR actually fill, and which stay empty?". The tier and the prose description are curated client-side; only the live measurements live here, so the page can never drift from what the corpus really contains. */
        MetadataFieldsResponse: {
            /** @description One entry per observed Tier-B / Tier-C field, ordered by field name. */
            fields: components["schemas"]["MetadataFieldStat"][];
        };
        /** @description Per-channel discovery telemetry for one source (Phase 122g / ADR-031). One entry per declared channel — sitemap, rss, html_sitemap, archive_index, or any future platform-class channel (timeline, subreddit, hashtag_stream, etc.). The shape is platform-agnostic by design so cross-platform comparability of coverage requires no schema work per new crawler binary. */
        DiscoveryCoveragePerChannel: {
            /**
             * @description Discovery channel identifier. Web-crawler channels today: `sitemap`, `rss`, `html_sitemap`, `archive_index`. Future platform crawlers contribute their own channel labels.
             * @example archive_index
             */
            channel: string;
            /**
             * @description Raw URL count surfaced by this channel in the most recent discovery pass (pre-dedup across channels).
             * @example 247
             */
            lastRunUrlsDiscovered: number;
            /**
             * @description URLs unique to this channel after the URL-union dedup across every declared channel. When sitemap + html_sitemap both yield URL X, sitemap (first) gets +1 here and html_sitemap (second) gets +0; the raw discovered count reflects what the channel actually surfaced.
             * @example 198
             */
            lastRunUrlsAfterDedup: number;
            /**
             * @description Trailing-window average (default last 30 days) of `urls_discovered` for this channel. Lets the dashboard contextualise a single run's number against the source's typical surface size.
             * @example 244.7
             */
            averageUrlsDiscoveredPerRun: number;
            /**
             * @description True iff the source's `expected_floor_per_run` has been below threshold for two consecutive runs (Phase 122g two-strike gate). The flag clears the first run back at or above floor.
             * @example false
             */
            underflowAlertActive: boolean;
            /**
             * @description Phase 148d (WP-007 §4.1) — the publisher-declared, in-window inventory this channel advertised, measured at its parse boundary BEFORE AĒR's filters. The completeness denominator (`completeness = collected / declared`). `null` for pre-148d rows (never measured) → treated as indeterminate.
             * @example 411
             */
            declared?: number | null;
            /**
             * @description Phase 148d (WP-007 §3/§5) — true when `declared` is only a LOWER BOUND and cannot be trusted as the full in-window inventory: a fetch/parse error swallowed entries, a walk/fetch cap truncated the channel, or the channel is structurally dateless (e.g. `html_sitemap`) / surfaced advertised-but-undatable content. When true, completeness for this channel is reported as indeterminate, never a clean ratio and never 100 %.
             * @example false
             */
            declaredIndeterminate?: boolean;
        };
        /**
         * @description Phase 148d (WP-007 §5) — the per-source collection funnel for the most recent crawl run, so a per-source conversion gap (e.g. franceinfo's declared→Gold ratio versus tagesschau's) is attributable stage-by-stage instead of left as a silent artifact (WP-007 §2).
         *     The channel-attributable head (declared → discovered → after_dedup) is in `perChannel`; once URLs merge into one crawl list, channel attribution is gone, so these spider stages are PER SOURCE. The tail (extracted → Gold) is reconciled at read-time against the current ClickHouse Gold row count for the source — no run_id is propagated Bronze→Silver→Gold.
         *     `present` is false when no funnel run has been recorded yet (a never-crawled source or a crawl that predates Phase 148d); the dashboard shows the head-of-funnel discovery telemetry alone in that case.
         */
        CollectionFunnel: {
            /**
             * @description True iff a funnel run has been recorded for this source.
             * @example true
             */
            present: boolean;
            /**
             * @description After-dedup URL count handed to the spider (funnel entry point).
             * @example 443
             */
            discovered?: number;
            /**
             * @description Dropped by the url_filter / IgnoreRequest (fails passed_filters).
             * @example 4
             */
            urlFiltered?: number;
            /**
             * @description Skipped by conditional-GET avoidance — already held from a prior run. NOT a collection loss; disclosed separately so a re-crawl that submits zero new documents reads as "fully held", never "0 % complete".
             * @example 0
             */
            alreadyCollected?: number;
            /**
             * @description HTTP responses received (transport failures excluded).
             * @example 439
             */
            fetched?: number;
            /**
             * @description 304 Not Modified responses (fetched, unchanged, already held).
             * @example 0
             */
            notModified?: number;
            /**
             * @description Dropped by content-type / empty-body guard (fails passed_filters).
             * @example 2
             */
            contentDropped?: number;
            /**
             * @description Dropped by the min_word_count gate — the Layer-3 over-collection (non-article) signal (WP-007 §4.3). Disclosed, never silently dropped.
             * @example 44
             */
            thinContentDropped?: number;
            /**
             * @description Reached Bronze (feeds extracted → Gold downstream).
             * @example 393
             */
            submitted?: number;
            /**
             * @description Non-200 / build / submit / transport failures.
             * @example 0
             */
            errored?: number;
            /**
             * @description Distinct articles for this source currently in ClickHouse Gold — the reconciled funnel tail (extracted → Gold). A point-in-time count, not scoped to a single run.
             * @example 333
             */
            goldRows?: number;
            /**
             * @description Layer-3 (WP-007 §4.3) — goldRows / submitted: the share of Bronze-submitted documents that became analysable Gold articles. Null when submitted is 0.
             * @example 0.847
             */
            extractionSuccessRate?: number | null;
            /**
             * @description Layer-3 (WP-007 §4.3) — thinContentDropped / fetched: the share of fetched pages that were non-article (thin) content. A high rate is an over-collection signal (the mirror image of an under-collection gap). Null when fetched is 0.
             * @example 0.1
             */
            nonArticleRate?: number | null;
        };
        /**
         * @description Per-source discovery telemetry payload for the Phase 122g `GET /sources/{sourceId}/discovery-coverage` endpoint. Sibling to `GET /sources/{sourceId}/metadata-coverage` (Phase 122f) — both surface the publisher's emission posture as a queryable runtime signal, supporting the WP-006 §6 reflexive-architecture status-disclosure principle.
         *     Recorded in ADR-031 (DiscoveryProtocol Contract for Multi-Channel Source Discovery). The schema is platform-agnostic so future Twitter / Reddit / Mastodon / YouTube crawlers feed the same endpoint without schema work.
         */
        DiscoveryCoverageResponse: {
            /**
             * @description Canonical source identifier (e.g. `tagesschau`).
             * @example tagesschau
             */
            sourceId: string;
            /**
             * @description Trailing window over which `averageUrlsDiscoveredPerRun` and the underflow detection are computed. Defaults to the probe's `time_window_days` (Phase 122b / 122e A20 — currently 7 for Probe 0).
             * @example 30
             */
            windowDays: number;
            /**
             * @description Source-declared minimum URL count per discovery run (Phase 122g). `null` when the operator hasn't yet set a floor for this source — no underflow alerting in that case.
             * @example 200
             */
            expectedFloorPerRun?: number | null;
            /** @description One entry per declared channel. Includes channels that yielded zero URLs in the last run (the zero is itself a signal). */
            perChannel: components["schemas"]["DiscoveryCoveragePerChannel"][];
            /**
             * @description Sum of `urls_discovered` across every channel for the most recent discovery pass (pre-dedup).
             * @example 1114
             */
            totalUrlsDiscoveredLastRun: number;
            /**
             * @description Sum of `urls_after_dedup` across every channel — the unique URL set the crawler will fetch. Compared against `expectedFloorPerRun` for the underflow gate.
             * @example 1006
             */
            uniqueUrlsAfterDedupLastRun: number;
            /**
             * @description True iff the per-source two-consecutive-runs underflow gate has fired. Same field as on each channel's record but at source level — surfaces the alert state directly to the dashboard panel.
             * @example false
             */
            underflowAlertActive: boolean;
            /**
             * @description Phase 148d (WP-007 §4.1) — sum of `declared` across the channels that supplied a TRUSTWORTHY denominator in the most recent run (declared not null AND not `declaredIndeterminate`). The completeness denominator. `null` when no channel supplied a measurable denominator.
             * @example 411
             */
            declaredTotalLastRun?: number | null;
            /**
             * @description Phase 148d (WP-007 §4.1, §3) — `goldRows / declaredTotalLastRun`: the share of the publisher-declared in-window inventory (over the MEASURABLE channels) that reached analysable Gold. This is "N % complete for this period against its declared channels"; any `declaredIndeterminate` channels are the named remainder (`indeterminateChannelCount`), not folded into this ratio. `null` when `completenessIndeterminate` is true — AĒR refuses to invent a ratio it cannot back, and never reports 100 %.
             * @example 0.81
             */
            completeness?: number | null;
            /**
             * @description Phase 148d (WP-007 §3/§5) — true when NO channel supplied a trustworthy denominator (`declaredTotalLastRun` is null/zero), so a completeness ratio cannot be formed at all. A source with at least one measurable channel reports a completeness figure even if other channels are indeterminate (those are disclosed as the named remainder, never silently dropped and never silently treated as 100 %).
             * @example false
             */
            completenessIndeterminate?: boolean;
            /**
             * @description Phase 148d (WP-007 §3) — how many channels in the last run had an indeterminate (lower-bound) denominator. The "named remainder": when > 0, the completeness figure is against the measurable channels only, and this count is the un-captured surface AĒR discloses rather than hides.
             * @example 1
             */
            indeterminateChannelCount?: number;
            funnel?: components["schemas"]["CollectionFunnel"];
        };
        /** @description One row of the aggregated revision-activity surface. Each entry rolls up `aer_gold.article_revisions` by (source, bucket) where the bucket is the time-grain produced by the requested resolution. Phase 122d.0 (ADR-032). Used by the `revision_activity` (Aleph snapshot) and `revision_timeline` (Episteme over-time) cells. */
        RevisionActivityEntry: {
            /**
             * @description Canonical source identifier (e.g. `tagesschau`).
             * @example tagesschau
             */
            source: string;
            /**
             * Format: date-time
             * @description The aggregation bucket start, normalised to the requested resolution. For the synchronic snapshot cell the BFF collapses to a single bucket spanning the whole window; for the diachronic time-series the bucket is daily/weekly/monthly per the `?resolution=` parameter.
             * @example 2026-05-01 00:00:00+00:00
             */
            bucket: string;
            /**
             * @description Count of revision rows in this (source, bucket) cell. CDX snapshots and republication-trigger rows are both included; the `byTrigger` breakdown distinguishes them.
             * @example 42
             */
            revisions: number;
            /**
             * @description Distinct article_ids contributing at least one revision in this cell. Tightly bounded by `revisions` from above.
             * @example 18
             */
            articlesAffected: number;
            /**
             * @description Per-revision-trigger breakdown. Keys are the allowed `revision_trigger` vocabulary (`cdx_snapshot`, `republication_trigger`, `unknown`); missing keys mean zero. The sum across keys equals `revisions`.
             * @example {
             *       "cdx_snapshot": 30,
             *       "republication_trigger": 12
             *     }
             */
            byTrigger?: {
                [key: string]: number;
            };
        };
        /** @description Aggregation response for the Silent-Edit Observability cells (Phase 122d.0 / ADR-032). Read from `aer_gold.article_revisions` with the same scope+window grammar as every other Gold endpoint. */
        RevisionActivityResponse: {
            /**
             * @description Echoed scope of the request.
             * @enum {string}
             */
            scope: "probe" | "source";
            /** @description Echoed scope identifier. */
            scopeId: string;
            /**
             * @description Aggregation grain. `snapshot` collapses to a single bucket spanning the whole window (Aleph cell); the others bucket the window on the requested grain (Episteme cell).
             * @enum {string}
             */
            resolution: "snapshot" | "daily" | "weekly" | "monthly";
            /**
             * Format: date-time
             * @description Echoed inclusive window start.
             */
            windowStart?: string;
            /**
             * Format: date-time
             * @description Echoed exclusive window end.
             */
            windowEnd?: string;
            /** @description One entry per (source, bucket). Sources with zero rows in the window do NOT appear — absence is itself a signal and the dashboard renders the empty row from the scope membership, not from this list. */
            entries: components["schemas"]["RevisionActivityEntry"][];
        };
        /** @description One row of the Silent-Edit Discourse Shift surface (Phase 122d.3). Rolls up `aer_gold.article_revisions` by (source, bucket) over the edit pairs whose discourse-shift deltas were computed (`deltas_computed=true` AND the pair is a real edit). Backs the `revision_discourse_shift` Episteme trajectory cell. All values are provisional engineering signals (`validation_status=unvalidated`). */
        RevisionDiscourseShiftEntry: {
            /**
             * @description Canonical source identifier (e.g. `tagesschau`).
             * @example tagesschau
             */
            source: string;
            /**
             * Format: date-time
             * @description Aggregation bucket start, normalised to the requested resolution (single window-spanning bucket for `snapshot`; daily/weekly/monthly otherwise).
             * @example 2026-05-01 00:00:00+00:00
             */
            bucket: string;
            /**
             * @description Count of editorial edits in this cell that have computed deltas (the denominator of the averages below).
             * @example 12
             */
            editsWithDeltas: number;
            /**
             * Format: double
             * @description Mean sentiment change across the cell's edits, later-in-time minus earlier-in-time. Positive = the source's edits trended more positive in this bucket. Zero when `editsWithDeltas=0`.
             * @example 0.08
             */
            avgSentimentDelta: number;
            /**
             * Format: double
             * @description Directional SUM of sentiment deltas across the cell's edits — the net editorial sentiment movement (distinct from the mean: a few large edits dominate). Zero when `editsWithDeltas=0`.
             * @example 0.96
             */
            netSentimentDrift: number;
            /**
             * Format: double
             * @description Mean semantic-shift magnitude (E5 cosine distance) across the cell's edits. Higher = edits moved the article's meaning more.
             * @example 0.21
             */
            avgTopicShift: number;
            /**
             * @description Total named-entity spans added across the cell's edits.
             * @example 7
             */
            entitiesAddedTotal: number;
            /**
             * @description Total named-entity spans removed across the cell's edits.
             * @example 3
             */
            entitiesRemovedTotal: number;
        };
        /** @description Aggregation response for the Silent-Edit Discourse Shift cell (Phase 122d.3). Read from `aer_gold.article_revisions` with the same scope+window+resolution grammar as `/revisions`. One entry per (source, bucket); cells with no computed-delta edits in the window do NOT appear (absence is rendered from scope membership, not this list). */
        RevisionDiscourseShiftResponse: {
            /**
             * @description Echoed scope of the request.
             * @enum {string}
             */
            scope: "probe" | "source";
            /** @description Echoed scope identifier. */
            scopeId: string;
            /**
             * @description Aggregation grain. `snapshot` collapses to a single bucket spanning the whole window; the others bucket the window on the requested grain (the Episteme trajectory).
             * @enum {string}
             */
            resolution: "snapshot" | "daily" | "weekly" | "monthly";
            /**
             * Format: date-time
             * @description Echoed inclusive window start.
             */
            windowStart?: string;
            /**
             * Format: date-time
             * @description Echoed exclusive window end.
             */
            windowEnd?: string;
            /** @description One entry per (source, bucket) with computed-delta edits. */
            entries: components["schemas"]["RevisionDiscourseShiftEntry"][];
        };
        /** @description One coordinated-edit cluster (Phase 122d.3, Rhizome). A cluster is a (time bucket, entity) where ≥ `minSources` distinct sources each made a computed-delta edit that added or removed that entity within the bucket — a cross-source temporal coincidence on the same entity. A relational signal, NOT a causal claim: co-occurrence in time is disclosed, not interpreted (WP-003 §5). Provisional (`validation_status=unvalidated`). */
        RevisionEditClusterEntry: {
            /**
             * Format: date-time
             * @description Aggregation bucket start (per the requested resolution).
             */
            bucket: string;
            /**
             * @description The named-entity surface span that ≥ `minSources` sources added or removed in this bucket.
             * @example Selenskyj
             */
            entity: string;
            /**
             * @description The distinct sources that co-edited this entity in the bucket (length ≥ the requested `minSources`).
             * @example [
             *       "tagesschau",
             *       "bundesregierung"
             *     ]
             */
            sources: string[];
            /**
             * @description Total (edit, entity) incidences in the cluster across all its sources — how many edits touched this entity in the bucket.
             * @example 4
             */
            editCount: number;
            /**
             * Format: double
             * @description Mean semantic-shift magnitude (E5 cosine distance) of the cluster's edits — how much those edits moved the articles' meaning.
             * @example 0.18
             */
            avgTopicShift: number;
        };
        /** @description Coordinated-edit-cluster response (Phase 122d.3, Rhizome). Surfaces cross-source temporally-clustered silent edits on shared entities, read from `aer_gold.article_revisions` with the same scope/window/resolution grammar as `/revisions`. Only edits with computed deltas contribute. The scope must resolve to ≥ 2 sources for any cluster to be possible (a single-source scope returns an empty list). */
        RevisionEditClustersResponse: {
            /**
             * @description Echoed scope of the request.
             * @enum {string}
             */
            scope: "probe" | "source";
            /** @description Echoed scope identifier. */
            scopeId: string;
            /**
             * @description The time-bucket grain coordinated edits are grouped within.
             * @enum {string}
             */
            resolution: "snapshot" | "daily" | "weekly" | "monthly";
            /** @description The minimum number of distinct co-editing sources for a (bucket, entity) to qualify as a cluster (echoed; default 2). */
            minSources: number;
            /**
             * Format: date-time
             * @description Echoed inclusive window start.
             */
            windowStart?: string;
            /**
             * Format: date-time
             * @description Echoed exclusive window end.
             */
            windowEnd?: string;
            /** @description Coordinated-edit clusters, most-edited first. Empty when no entity was co-edited by ≥ `minSources` sources in any bucket. */
            clusters: components["schemas"]["RevisionEditClusterEntry"][];
        };
        /** @description Per-article revision chain for the L5 Evidence Reader (Phase 122d.0). Returns the ordered list of detected revisions for one article — Wayback CDX snapshots and publisher-side republication-trigger events. The endpoint is subject to the Silver-eligibility gate inherited from `GET /articles/{id}`: a source whose `silver_eligible=false` returns 403 with the same refusal payload. */
        ArticleRevisionsResponse: {
            /** @description SHA-256 article identifier. */
            articleId: string;
            /** @description Canonical source of the article. */
            source: string;
            /**
             * @description Wayback CDX coverage signal for THIS article. `ok` = ≥ 1 CDX snapshot was captured; `no_snapshots` = the URL is not yet archived; `failed` = the lookup attempted but errored (timeout, rate-limit, network); `skipped` = canonical URL missing; `disabled` = the worker had the CDX integration off; empty string = pre-Phase-122d.0 data with no provenance recorded.
             * @enum {string}
             */
            lookupStatus: "ok" | "no_snapshots" | "failed" | "skipped" | "disabled" | "";
            /** @description Ordered list of revisions (snapshot_at ascending). An article with a republication-trigger but no CDX snapshots still emits one entry whose `trigger='republication_trigger'`. An article with `lookupStatus='no_snapshots'` and no republication trigger returns an empty list. */
            revisions: {
                /**
                 * Format: date-time
                 * @description When this revision was observed.
                 */
                snapshotAt: string;
                /** @description Content-body digest at this snapshot. For CDX snapshots, the value the Wayback Machine recorded (SHA-1 hex). For republication-trigger rows, the SHA-1 of the current cleaned-text. */
                contentHash: string;
                /** @description `contentHash` of the immediately preceding entry, or empty string for the chain head. */
                prevContentHash?: string;
                /** @description Zero-based position in the chain. */
                revisionIndex: number;
                /**
                 * Format: double
                 * @description Hours between this entry and the preceding one. Zero for the chain head.
                 */
                timeSincePrevHours?: number;
                /**
                 * @description Which mechanism produced this row.
                 * @enum {string}
                 */
                trigger: "cdx_snapshot" | "republication_trigger" | "unknown";
                /** @description Internet Archive playback URL for CDX snapshots. Empty string for republication-trigger rows (no archive page exists yet). */
                archiveUrl?: string;
                /**
                 * @description Editorial status of the diff for the pair ending at THIS revision (revisionIndex-1 → revisionIndex; for the chain head, current article body → this snapshot). `changed` = paragraph-level or headline edits were detected; `identical` = both snapshots parse to the same article body after extraction (a re-archival without an editorial change); `pending` = the background diff sweep has not yet computed this pair. Lets the L5 reader step the slider over only the editorial versions and disclose how many captures were identical re-archivals (Phase 133 — derived from `aer_gold.article_revisions.diff_paragraphs`, no extra query).
                 * @enum {string}
                 */
                diffStatus: "changed" | "identical" | "pending";
                /** @description Phase 122d.3 — whether the discourse-shift deltas below were computed for the pair ending at THIS revision. `false` = identical re-archival, language unknown, or not yet re-extracted; the delta fields then carry defaults and MUST NOT be read as measurements. Provisional engineering signal (`validation_status=unvalidated`). */
                deltasComputed: boolean;
                /**
                 * Format: double
                 * @description Change in the multilingual-backbone sentiment scalar across the edit, computed **later-in-time minus earlier-in-time**. For the chain-head pair (current article body vs newest snapshot) "later" is the CURRENT article — a positive value means the article reads more positively now than at the snapshot. Omitted/zero when `deltasComputed=false`. Do not re-invert the sign client-side.
                 */
                sentimentDelta?: number;
                /** @description Named-entity surface spans present in the LATER text but not the earlier one (set difference). Empty when `deltasComputed=false`. */
                entitiesAdded?: string[];
                /** @description Named-entity surface spans present in the EARLIER text but not the later one. Empty when `deltasComputed=false`. */
                entitiesRemoved?: string[];
                /**
                 * Format: double
                 * @description Semantic-shift magnitude — cosine distance between the two snapshots' multilingual-E5 embeddings (0 = identical semantic vector). A SEMANTIC shift, NOT a topic-label switch. Zero when not computed.
                 */
                topicShiftScore?: number;
            }[];
        };
        /**
         * @description Paragraph-aligned diff for one article-revision pair (Phase 122d.1 / ADR-032 amendment, BUG-11 amendment). Two pair kinds exist:
         *
         *       * `revisionIndex=0` (chain_head): pair is `current Silver body →
         *         Wayback[0]` — answers "what has the publisher changed since
         *         the last Wayback archive". Makes articles with chainLength=1
         *         diffable.
         *       * `revisionIndex>0` (mid_chain): pair is `Wayback[n-1] →
         *         Wayback[n]` — diff between two consecutive archived snapshots.
         *
         *     Equal paragraphs are intentionally not included — the payload is sparse; the consumer should not expect a complete document reconstruction. When the two snapshots parse to identical paragraph content, `identical=true` and `diffParagraphs` is empty (BUG-10 distinct from `404 pending`).
         */
        ArticleRevisionDiff: {
            /** @description SHA-256 article identifier. */
            articleId: string;
            /** @description Zero-based position of the LATER snapshot. `0` is the chain head (current Silver → Wayback[0]); values > 0 compare consecutive Wayback snapshots. */
            revisionIndex: number;
            /**
             * @description `chain_head` when `revisionIndex=0` (Silver-now → Wayback[0]). `mid_chain` otherwise.
             * @enum {string}
             */
            pairKind: "chain_head" | "mid_chain";
            /** @description True when the worker computed the diff and found the two snapshots parsed to identical paragraph content. The `diffParagraphs` array is empty in that case — the dashboard surfaces this as "snapshots identical after extraction", distinct from a 404 "diff pending". */
            identical: boolean;
            /**
             * Format: date-time
             * @description Wayback capture time of the earlier snapshot. Null for `chain_head` pairs (the "before" side is the live Silver body, with no archive timestamp).
             */
            snapshotAtBefore?: string | null;
            /**
             * Format: date-time
             * @description Wayback capture time of the later snapshot.
             */
            snapshotAtAfter: string;
            /** @description True iff the article's headline (extracted via trafilatura metadata with a Wayback-toolbar-stripping fallback) differs between the two snapshots. Engineering-derived signal — not a methodological canon; see ADR-037 for the disclosure prose. */
            headlineChanged: boolean;
            /** @description Title at the earlier snapshot; empty when `headlineChanged=false`. */
            headlineBefore?: string;
            /** @description Title at the later snapshot; empty when `headlineChanged=false`. */
            headlineAfter?: string;
            /** @description Ordered list of paragraph-level ops. Equal paragraphs are not included — the payload is sparse. Op vocabulary: `add` (paragraph inserted), `del` (paragraph removed), `mod` (paragraph modified — similarity ≥ 0.4 between before and after). */
            diffParagraphs: {
                /**
                 * @description Op type.
                 * @enum {string}
                 */
                op: "add" | "del" | "mod";
                /** @description Original paragraph text (`del`, `mod`). */
                before?: string;
                /** @description Replacement paragraph text (`add`, `mod`). */
                after?: string;
            }[];
        };
        /** @description Paginated list of articles with at least one revision in the active window (Phase 122d.1 / ADR-032 amendment). Powers the Workbench drill-down from a `revision_activity` bar or a `revision_timeline` bucket-point into the article list backing the signal. Cursor-based pagination identical to `/sources/{id}/articles` for consistency. */
        RevisionsArticlesPage: {
            items: {
                articleId: string;
                source: string;
                /**
                 * Format: date-time
                 * @description Article's published date.
                 */
                timestamp: string;
                language?: string | null;
                wordCount?: number | null;
                /** @description Total archived CAPTURES detected for this article in the window (Wayback CDX snapshots + republication triggers). This is a tooling/coverage count — most captures are identical re-archivals, NOT source edits. For "how often was the article actually changed", use `editorialChangeCount`. */
                chainLength: number;
                /** @description Number of captures whose content differs editorially from the previous one (paragraph/headline change after extraction) — i.e. how many times the source actually revised the article, as witnessed. Excludes identical re-archivals. A lower bound while some pairs are still being diffed (Phase 133). */
                editorialChangeCount: number;
                /** @description True iff at least one revision in the chain reports `headline_changed=true`. */
                hasHeadlineChange: boolean;
                /**
                 * Format: date-time
                 * @description Timestamp of the most recent revision in the chain.
                 */
                latestRevisionAt?: string;
            }[];
            hasMore: boolean;
            /** @description Opaque cursor token; pass back on the next request. */
            nextCursor?: string;
        };
        /** @description The authenticated user, as returned by /auth/login, /auth/accept-invite and /auth/me. Privacy-minimal (ADR-040): identity only — never any record of what the user analyses. */
        AuthUser: {
            /** @description Opaque user id (server-side UUID, surfaced as a string). */
            id: string;
            /** Format: email */
            email: string;
            /** @description RBAC role (ADR-040) — one of `admin`, `researcher`. */
            role: string;
            /** @description Account status — one of `invited`, `active`, `suspended`. */
            status: string;
        };
        /** @description Machine-readable authentication / authorization error (ADR-040). Distinct from RefusalPayload / Error: the presence of a `code` and the absence of a methodological-refusal envelope (gate / workingPaperAnchor) is how the dashboard tells an auth error apart from a methodological 4xx refusal. A 401 routes the client to /login; a 403 `forbidden_*` renders an explained access surface. */
        AuthError: {
            /** @description Machine-readable auth error code, e.g. `invalid_credentials`, `unauthenticated`, `forbidden_role`, `forbidden_not_shared`, `invalid_token`, `consent_required`, `weak_password`. */
            code: string;
            /** @description Human-readable, non-leaking explanation. */
            message: string;
        };
        LoginRequest: {
            /** Format: email */
            email: string;
            /** @description Plaintext password, verified against the argon2id hash server-side. */
            password: string;
        };
        /** @description Invite acceptance (ADR-040). The single-use invite token is exchanged for an activated account: the invitee sets their own password and records the responsible-use agreement (LICENSE §3.2.b). No password ever travels by email — only the token does. */
        AcceptInviteRequest: {
            /** @description The single-use invite token from the invitation link. */
            token: string;
            /** @description The password the invitee chooses. Minimum length re-checked server-side. */
            password: string;
            /** @description Must be true. Records the LICENSE §3.2.b responsible-use consent at activation. A false/absent value returns 400 `consent_required`. */
            acceptResponsibleUse: boolean;
        };
        /** @description Self-service password-reset request (ADR-040). The response is always 202 regardless of whether the email maps to an account — no user enumeration. */
        ForgotPasswordRequest: {
            /** Format: email */
            email: string;
        };
        /** @description Completes a password reset (ADR-040) with a single-use reset token. On success every existing session for the user is invalidated. */
        ResetPasswordRequest: {
            /** @description The single-use password-reset token from the reset link. */
            token: string;
            /** @description The new password. Minimum length re-checked server-side. */
            password: string;
        };
        /** @description Password change for an authenticated user who knows their current password (ADR-040). No email round-trip. Other sessions are invalidated on success. */
        ChangePasswordRequest: {
            currentPassword: string;
            newPassword: string;
        };
        /** @description A user row as seen by an admin (ADR-040, Phase 134). */
        AdminUser: {
            /** @description Opaque user id. */
            id: string;
            /** Format: email */
            email: string;
            /** @description One of `admin`, `researcher`. */
            role: string;
            /** @description One of `invited`, `active`, `suspended`. */
            status: string;
            /** Format: date-time */
            createdAt: string;
        };
        AdminUsersList: {
            users: components["schemas"]["AdminUser"][];
        };
        /** @description Admin-initiated account creation (ADR-040). Creates an invited user and issues a single-use invite token. No open self-registration (LICENSE §3.2). */
        CreateUserRequest: {
            /** Format: email */
            email: string;
            /** @description One of `admin`, `researcher`. Validated server-side. */
            role: string;
        };
        /** @description The one-time link produced by an admin action (invite or admin-initiated password reset). The same link is dispatched through the configured transactional-email relay (Phase 153) and ALSO returned here as the break-glass channel: when `delivered` is false the admin delivers `link` manually (relay unconfigured or send failed). */
        AdminActionLink: {
            userId: string;
            /** Format: email */
            email: string;
            /** @description One of `invite`, `password_reset`. */
            kind: string;
            /** @description The accept-invite / reset-password link carrying the single-use token. */
            link: string;
            /** @description Whether the link was dispatched through a real transactional-email relay (Phase 153). False when the relay is not configured (LogSender fallback) or the send failed — the admin must then deliver `link` manually. */
            delivered?: boolean;
        };
        /** @description Everything AĒR stores about a user (DSGVO Art. 15 / 20 — right of access + portability). Privacy-minimal by design (ADR-040 / Manifesto §VI / WP-006 §7): identity + consent + session metadata only. AĒR keeps NO record of what a user analyses beyond explicitly saved analyses (Phase 135) — the sparseness of this export is the point. */
        UserDataExport: {
            id: string;
            /** Format: email */
            email: string;
            role: string;
            status: string;
            /** Format: date-time */
            createdAt: string;
            /**
             * Format: date-time
             * @description LICENSE §3.2.b consent timestamp; null if never activated.
             */
            responsibleUseAcceptedAt?: string | null;
            /** @description Count of the user's current non-revoked, unexpired sessions. */
            activeSessionCount: number;
            /**
             * Format: date-time
             * @description Most recent activity across the user's sessions; null if none.
             */
            lastSeenAt?: string | null;
        };
        /** @description Opaque WebAuthn ceremony options (ADR-040). The BFF passes the go-webauthn CredentialCreation / CredentialAssertion options straight through to the browser's `navigator.credentials` API; the schema is intentionally freeform. */
        WebAuthnOptions: {
            [key: string]: unknown;
        };
        /** @description Opaque WebAuthn ceremony response (ADR-040) — the raw credential the browser returns from `navigator.credentials.create()` / `.get()`. Parsed and verified by the go-webauthn library server-side. */
        WebAuthnResponse: {
            [key: string]: unknown;
        };
        /** @description A registered passkey, for display (no key material) (ADR-040). */
        WebAuthnCredential: {
            /** @description Opaque credential row id (used to delete it). */
            id: string;
            name?: string | null;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            lastUsedAt?: string | null;
        };
        WebAuthnCredentialsList: {
            credentials: components["schemas"]["WebAuthnCredential"][];
        };
        AssertionResult: {
            /** @description True when the passkey assertion verified successfully. */
            verified: boolean;
        };
        /** @description A saved Workbench analysis as shown in the list (no state blob) (Phase 135). */
        SavedAnalysisListItem: {
            id: string;
            name: string;
            description: string;
            /**
             * Format: email
             * @description The creator's email.
             */
            ownerEmail: string;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
            /** @description The viewer's effective permission — `editable` or `readable`. */
            permission: string;
            /** @description True when the viewer is the owner. */
            owned: boolean;
        };
        SavedAnalysisList: {
            analyses: components["schemas"]["SavedAnalysisListItem"][];
        };
        /** @description A saved Workbench analysis with its serialized state (Phase 135). */
        SavedAnalysis: {
            id: string;
            name: string;
            description: string;
            /** @description The serialized Workbench URL query */
            state: string;
            /** Format: email */
            ownerEmail: string;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
            /** @description `editable` or `readable`. */
            permission: string;
            owned: boolean;
        };
        CreateAnalysisRequest: {
            name: string;
            description?: string;
            /** @description The serialized Workbench URL query to persist. */
            state: string;
        };
        /** @description Patch a saved analysis. Fields are optional; omitted fields keep their value. */
        UpdateAnalysisRequest: {
            name?: string;
            description?: string;
            state?: string;
        };
        AnalysisShare: {
            granteeId: string;
            /** Format: email */
            email: string;
            /** @description True grants edit; false is read-only. */
            canEdit: boolean;
        };
        AnalysisShareList: {
            shares: components["schemas"]["AnalysisShare"][];
        };
        /** @description Grant a specific user (by email) access to an analysis (Phase 135). No share-with-all. */
        CreateShareRequest: {
            /** Format: email */
            email: string;
            /**
             * @description True grants edit; default read-only.
             * @default false
             */
            canEdit: boolean;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    postAuthLogin: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoginRequest"];
            };
        };
        responses: {
            /** @description Authenticated. Sets the session cookie; returns the user. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthUser"];
                };
            };
            /** @description Malformed request body. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Invalid credentials, or the account is not active. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Too many failed login attempts; backoff in effect (ADR-040 brute-force throttle). */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthLogout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Logged out. Clears the session cookie. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAuthMe: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The authenticated user. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthUser"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    deleteAuthMe: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Account deleted; session cookie cleared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAuthMeExport: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The user's data export. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UserDataExport"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAuthSessions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The user's active sessions. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description Active (non-revoked, unexpired) sessions, most-recently-seen first. */
                        sessions: {
                            /**
                             * Format: date-time
                             * @description When this session was created (login time).
                             */
                            createdAt: string;
                            /**
                             * Format: date-time
                             * @description When this session was last used.
                             */
                            lastSeenAt: string;
                            /** @description The User-Agent recorded at login (may be empty). */
                            userAgent?: string;
                            /** @description True for the session making this request. */
                            current: boolean;
                        }[];
                    };
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    deleteAuthSessions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All sessions revoked; session cookie cleared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthAcceptInvite: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AcceptInviteRequest"];
            };
        };
        responses: {
            /** @description Account activated. Sets the session cookie; returns the user. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthUser"];
                };
            };
            /** @description Invalid/expired/consumed token, missing consent (`consent_required`), or a password that fails the policy (`weak_password`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthForgotPassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ForgotPasswordRequest"];
            };
        };
        responses: {
            /** @description Accepted. If an account exists, a reset link was dispatched. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthResetPassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResetPasswordRequest"];
            };
        };
        responses: {
            /** @description Password reset; all sessions invalidated. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Invalid/expired/consumed token (`invalid_token`) or a password that fails the policy (`weak_password`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthChangePassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ChangePasswordRequest"];
            };
        };
        responses: {
            /** @description Password changed; other sessions invalidated. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Password fails the policy (`weak_password`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Not authenticated, or the current password is wrong. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthWebauthnRegisterBegin: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Credential-creation options (pass to navigator.credentials.create). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebAuthnOptions"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthWebauthnRegisterFinish: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WebAuthnResponse"];
            };
        };
        responses: {
            /** @description Passkey registered. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebAuthnCredential"];
                };
            };
            /** @description Invalid or expired ceremony / attestation. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAuthWebauthnCredentials: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The user's passkeys. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebAuthnCredentialsList"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    deleteAuthWebauthnCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Passkey removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No such passkey for this user. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthWebauthnAssertBegin: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Assertion options (pass to navigator.credentials.get). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebAuthnOptions"];
                };
            };
            /** @description The user has no registered passkeys. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAuthWebauthnAssertFinish: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WebAuthnResponse"];
            };
        };
        responses: {
            /** @description Assertion verified. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssertionResult"];
                };
            };
            /** @description No valid session, or the assertion failed. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAdminUsers: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The user list. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminUsersList"];
                };
            };
            /** @description Caller is not an admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAdminUsers: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateUserRequest"];
            };
        };
        responses: {
            /** @description User invited. Returns the accept-invite link. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminActionLink"];
                };
            };
            /** @description Malformed body or invalid role. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Caller is not an admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description A user with this email already exists. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAdminUserSuspend: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description User suspended. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description An admin cannot suspend their own account. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Caller is not an admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No such user. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAdminUserReactivate: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description User reactivated. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Caller is not an admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No such user. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAdminUserResetPassword: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reset token issued. Returns the reset link. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminActionLink"];
                };
            };
            /** @description Caller is not an admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No such user. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAnalyses: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The visible analyses. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SavedAnalysisList"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAnalyses: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateAnalysisRequest"];
            };
        };
        responses: {
            /** @description Created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SavedAnalysisListItem"];
                };
            };
            /** @description Malformed body. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAnalysis: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The analysis. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SavedAnalysis"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Not found or not shared with you. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    deleteAnalysis: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Only the owner can delete. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    patchAnalysis: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateAnalysisRequest"];
            };
        };
        responses: {
            /** @description Updated analysis. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SavedAnalysis"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Not allowed to edit this analysis. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getAnalysisShares: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The grantees. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AnalysisShareList"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Only the owner can manage shares. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postAnalysisShare: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateShareRequest"];
            };
        };
        responses: {
            /** @description Shared. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AnalysisShare"];
                };
            };
            /** @description Malformed, or cannot share with yourself (`cannot_share_with_self`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Only the owner can share. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No account exists for that email (`grantee_not_found`). */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    deleteAnalysisShare: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
                granteeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No valid session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Only the owner can manage shares. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description No such grant. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthError"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetrics: {
        parameters: {
            query?: {
                /** @description Start date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                startDate?: string;
                /** @description End date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                endDate?: string;
                /** @description Filter metrics by data source (e.g., "wikipedia") */
                source?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Filter metrics by metric name (e.g., "word_count") */
                metricName?: string;
                /** @description Normalization mode for metric values. "raw" returns values as stored. "zscore" returns z-score normalized values: (value - baseline_mean) / baseline_std. "percentile" (Phase 115) returns within-(metric, source, language) percentile rank computed via ClickHouse window functions over the active query window. Both "zscore" and "percentile" require baseline data; cross-frame requests additionally require deviation-level equivalence validation, otherwise the request is refused with an HTTP 400 RefusalPayload (gate=metric_equivalence). */
                normalization?: "raw" | "zscore" | "percentile";
                /** @description Temporal aggregation resolution for the returned series. Maps to ClickHouse bucketing functions: 5min → toStartOfFiveMinute, hourly → toStartOfHour, daily → toStartOfDay, weekly → toStartOfWeek, monthly → toStartOfMonth. Wider buckets relax the per-request row cap proportionally. */
                resolution?: "5min" | "hourly" | "daily" | "weekly" | "monthly";
                /** @description When true (Phase 131), each data point additionally carries `stddev` — the Bessel-corrected sample standard deviation of the per-document metric values in the bucket — so the dashboard can render a ±1σ uncertainty band. Because the pre-aggregated resolution materialized views do not store a variance state, a spread-bearing request reads the raw `aer_gold.metrics` table at the requested bucket resolution. Mutually exclusive with `normalization` (z-score / percentile responses do not carry a raw spread); when both are set the spread is omitted. Defaults to false, preserving the byte-identical materialized-view fast path for every existing caller. */
                includeStddev?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Envelope containing the time-series data points and query metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description The aggregated time-series data points for the requested window. */
                        data: {
                            /**
                             * Format: date-time
                             * @description The UTC timestamp of the aggregation interval.
                             */
                            timestamp: string;
                            /**
                             * Format: double
                             * @description The aggregated sociological metric value.
                             */
                            value: number;
                            /** @description The data source that produced this metric (e.g., "tagesschau", "bundesregierung"). */
                            source: string;
                            /** @description The name of the metric (e.g., "word_count", "sentiment_score"). */
                            metricName: string;
                            /**
                             * Format: int64
                             * @description Number of gold-layer rows (i.e. per-document metric emissions) that contributed to `value` in this bucket. Distinct from `value`, which is the aggregated statistic (typically avg). Callers needing a document rate — e.g. the Atmosphere's per-probe pulse — must use `count / window_hours`, not the metric value itself, because most metrics store a per-document *measurement* (hour-of-day, sentiment, word count), not a document tally.
                             */
                            count?: number;
                            /**
                             * Format: double
                             * @description Sample standard deviation (Bessel-corrected) of the per-document metric values that contributed to `value` in this bucket. Populated only when the request set `includeStddev=true` (Phase 131); absent otherwise. Powers the time-series uncertainty band (±1σ around `value`). Computed from the raw `aer_gold.metrics` table — the pre-aggregated resolution MVs do not carry a variance state — so a spread-bearing request always reads the raw layer regardless of resolution. Buckets with fewer than two contributing rows report 0 (the sample stddev is undefined for n<2).
                             */
                            stddev?: number;
                        }[];
                        /** @description True when the time-series hit the server's row cap and the tail (the latest buckets) was dropped. The dashboard surfaces a "showing the earliest N buckets" note so a wide multi-source/no-metric-filter window is never silently presented as a complete series. Narrow the window, filter to a metric, or raise the resolution to clear it. */
                        truncated: boolean;
                        /**
                         * Format: int64
                         * @description Number of source rows that were dropped from the aggregation because their article had no matching language detection. Always 0 for raw (non-normalized) queries; may be non-zero for `normalization=zscore`, where the language-scoped baseline join requires a detected language. A non-zero value surfaces data-quality gaps without altering the aggregated payload.
                         */
                        excludedCount: number;
                    };
                };
            };
            /** @description Missing or invalid parameters; or a normalization request was refused by a methodological gate (Phase 115 cross-frame equivalence). When the 400 is a methodological refusal, the body's optional `gate`, `workingPaperAnchor`, and `alternatives` fields carry the same information a `RefusalPayload` would. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricsAvailable: {
        parameters: {
            query?: {
                /** @description Start date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                startDate?: string;
                /** @description End date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                endDate?: string;
                /** @description Language for the per-metric `displayLabel` (Task B). Defaults to "en". A metric with no catalogue entry in the locale yields a null displayLabel — the client then falls back to the machine name. */
                locale?: "en" | "de";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A list of available metrics with validation status. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AvailableMetric"][];
                };
            };
            /** @description Missing required parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricCorrelation: {
        parameters: {
            query: {
                /** @description Comma-separated list of metric names to include in the correlation matrix (e.g. `sentiment_score,word_count,entity_count`). Required; must contain between 2 and 10 names. */
                metrics: string;
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description NxN correlation matrix. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CorrelationMatrix"];
                };
            };
            /** @description Invalid scope, fewer than two metrics, or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getCorrelationLeadLag: {
        parameters: {
            query: {
                /** @description Metric name bound to the scatter's X position channel (e.g. `word_count`). Required. Only articles carrying both `xMetric` and `yMetric` in the window contribute a point. */
                xMetric: string;
                /** @description Metric name bound to the scatter's Y position channel (e.g. `sentiment_score_sentiws`). Required. Only articles carrying both `xMetric` and `yMetric` in the window contribute a point. */
                yMetric: string;
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Symmetric lag bound in hours (default 168, max 720). */
                maxLagHours?: number;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Lead-lag spectrum. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        xMetric: string;
                        yMetric: string;
                        /** @description Symmetric lag bound; `points` span -maxLagHours..+maxLagHours. */
                        maxLagHours: number;
                        /**
                         * Format: int64
                         * @description Overlapping hourly buckets at lag 0 (a sample-size proxy).
                         */
                        bucketCountAtZero?: number;
                        /** @description One entry per integer lag in [-maxLagHours, +maxLagHours]. */
                        points: {
                            lagHours: number;
                            /**
                             * Format: double
                             * @description Pearson correlation at this lag; null when too few overlapping buckets or zero variance.
                             */
                            correlation: number | null;
                        }[];
                        peakLagHours?: number | null;
                        /** Format: double */
                        peakCorrelation?: number | null;
                        scope?: string | null;
                        scopeId?: string | null;
                        /** Format: date-time */
                        windowStart?: string | null;
                        /** Format: date-time */
                        windowEnd?: string | null;
                    };
                };
            };
            /** @description Invalid scope/window, or a cross-frame equivalence refusal. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricParallelCoords: {
        parameters: {
            query: {
                /** @description Comma-separated list of metric names to include in the correlation matrix (e.g. `sentiment_score,word_count,entity_count`). Required; must contain between 2 and 10 names. */
                metrics: string;
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Maximum number of per-article points to return for the scatter cloud, ordered deterministically by article id. Server clamps values outside [1, 10000] to the nearest bound. When the in-window article set exceeds this cap the response sets `truncated=true` so the dashboard can surface a "showing N of M" note rather than implying the cloud is exhaustive. */
                maxPoints?: number;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Parallel-coordinates matrix. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description The metric axes, in order; each row's `values` aligns to this. */
                        metrics: string[];
                        rows: {
                            articleId: string;
                            source: string;
                            values: number[];
                        }[];
                        /** @description True when the per-article cap bit (showing the first N). */
                        truncated: boolean;
                        scope?: string | null;
                        scopeId?: string | null;
                        /** Format: date-time */
                        windowStart?: string | null;
                        /** Format: date-time */
                        windowEnd?: string | null;
                    };
                };
            };
            /** @description Invalid scope/window, fewer than two metrics, or a cross-frame refusal. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricProvenance: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The canonical metric name (e.g., "word_count"). */
                metricName: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The provenance metadata for the requested metric. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetricProvenance"];
                };
            };
            /** @description No provenance entry exists for the requested metric. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricDistribution: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description When set, the response additionally contains a `streams` array where each element carries the per-segment payload (same fields as the aggregate response). The aggregate fields (`bins`, `summary`, `cells`) are always included and reflect the full union scope. `source` produces one stream per resolved source; `probe` produces one stream per probe in `probeIds` (returns 400 when no probe IDs are resolved). Omitting this parameter preserves the current single-payload shape. */
                segmentBy?: "source" | "probe";
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Number of histogram bins to request for the distribution. Server clamps values outside [1, 200] to the nearest bound. */
                bins?: number;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path: {
                /** @description Metric name (e.g. `sentiment_score`). */
                metricName: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Distribution payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DistributionResponse"];
                };
            };
            /** @description Invalid scope, unknown probe / source, or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricHeatmap: {
        parameters: {
            query: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description When set, the response additionally contains a `streams` array where each element carries the per-segment payload (same fields as the aggregate response). The aggregate fields (`bins`, `summary`, `cells`) are always included and reflect the full union scope. `source` produces one stream per resolved source; `probe` produces one stream per probe in `probeIds` (returns 400 when no probe IDs are resolved). Omitting this parameter preserves the current single-payload shape. */
                segmentBy?: "source" | "probe";
                /** @description X-axis dimension for the heatmap. `dayOfWeek` and `hour` bin on the metric timestamp; `source` groups by source name; `entityLabel` joins against `aer_gold.entities`; `language` joins against `aer_gold.language_detections`. */
                xDimension: "dayOfWeek" | "hour" | "source" | "entityLabel" | "language";
                /** @description Y-axis dimension for the heatmap. Same enum as xDimension. */
                yDimension: "dayOfWeek" | "hour" | "source" | "entityLabel" | "language";
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
            };
            header?: never;
            path: {
                metricName: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Heatmap cells for the requested dimensions. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HeatmapResponse"];
                };
            };
            /** @description Invalid scope, dimensions, or window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetricScatter: {
        parameters: {
            query: {
                /** @description Metric name bound to the scatter's X position channel (e.g. `word_count`). Required. Only articles carrying both `xMetric` and `yMetric` in the window contribute a point. */
                xMetric: string;
                /** @description Metric name bound to the scatter's Y position channel (e.g. `sentiment_score_sentiws`). Required. Only articles carrying both `xMetric` and `yMetric` in the window contribute a point. */
                yMetric: string;
                /** @description Optional metric name bound to the point-size visual channel. When set, the point's `size` field carries this metric's per-article value (absent on articles lacking it). Omit to leave the size channel unbound. This is the forward-compatible seam for Phase 133+ metric chaining: additional bound channels extend the request without breaking the response shape. */
                sizeMetric?: string;
                /** @description Optional metric name bound to the point-colour visual channel. When set, the point's `color` field carries this metric's per-article value (absent on articles lacking it). Omit to leave the colour channel unbound. Like `sizeMetric`, this is a forward-compatible seam for Phase 133+ metric chaining. */
                colorMetric?: string;
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Maximum number of per-article points to return for the scatter cloud, ordered deterministically by article id. Server clamps values outside [1, 10000] to the nearest bound. When the in-window article set exceeds this cap the response sets `truncated=true` so the dashboard can surface a "showing N of M" note rather than implying the cloud is exhaustive. */
                maxPoints?: number;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-article scatter points with bound visual channels. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description Echo of the requested X-position metric. */
                        xMetric: string;
                        /** @description Echo of the requested Y-position metric. */
                        yMetric: string;
                        /** @description Echo of the requested size-channel metric, when bound. */
                        sizeMetric?: string;
                        /** @description Echo of the requested colour-channel metric, when bound. */
                        colorMetric?: string;
                        /** @description Resolved scope kind (`probe` or `source`). */
                        scope?: string;
                        scopeId?: string;
                        /** @description Per-article points, ordered by article id for determinism. */
                        points: {
                            /**
                             * Format: double
                             * @description Value of the request's `xMetric` for this article.
                             */
                            x: number;
                            /**
                             * Format: double
                             * @description Value of the request's `yMetric` for this article.
                             */
                            y: number;
                            /**
                             * Format: double
                             * @description Value of the request's `sizeMetric` for this article. Absent when the size channel is unbound or the article lacks that metric.
                             */
                            size?: number;
                            /**
                             * Format: double
                             * @description Value of the request's `colorMetric` for this article. Absent when the colour channel is unbound or the article lacks that metric.
                             */
                            color?: number;
                            /** @description The source that produced the article. */
                            source: string;
                            /** @description The article identifier, when available (used for the publication-ready data export so a point traces back to its document). */
                            articleId?: string;
                            /**
                             * Format: date-time
                             * @description The article's publication timestamp.
                             */
                            timestamp: string;
                        }[];
                        /** @description True when the in-window article set exceeded `maxPoints` and the point list was capped. The dashboard surfaces a "showing N of more" note. */
                        truncated: boolean;
                        /**
                         * Format: date-time
                         * @description Resolved lower window bound; null when the query was unbounded (whole dataset).
                         */
                        windowStart?: string | null;
                        /**
                         * Format: date-time
                         * @description Resolved upper window bound; null when unbounded.
                         */
                        windowEnd?: string | null;
                    };
                };
            };
            /** @description Invalid scope, missing position metric, or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getScopeAvailableMetrics: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Available (all-source) and partial (some-source) metric sets. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description The resolved source names the availability was computed over. */
                        scopedSources: string[];
                        /** @description Metric names that have at least one Gold row for EVERY scoped source in the window. These are the only metrics safe to bind on a panel spanning the whole scope. */
                        available: string[];
                        /** @description Metrics present for SOME but not all scoped sources, with the subset of sources that carry them. Surfaced as an explanatory hint, never offered for binding. */
                        partial: {
                            metricName: string;
                            /** @description The scoped sources that have data for this metric. */
                            sources: string[];
                        }[];
                        /** @description Metrics present in the window whose value is CONSTANT across the whole scope (exactly one distinct value) — present, but carrying no signal: a distribution / scatter / time-series over a constant is meaningless. The dashboard drops these from the metric picker, but they are disclosed here with the constant value so the omission is never silent (ADR-039 DISCLOSE-NEVER-COERCE — "present but constant → no signal", not absence). Self-maintaining: a future source that introduces real variance removes the metric from this list automatically, with no code change. Computed over the scope union, so a metric may appear here even while present in `available`/`partial` (those lists keep their pure "has data" semantics). */
                        degenerate?: {
                            metricName: string;
                            /** @description The single constant value observed across the scope. */
                            value: number;
                        }[];
                        /** @description Available metrics with ≥2 distinct values but a strongly DOMINANT value (near-constant in this scope: dominantShare ≥ the disclosed threshold) — e.g. a publisher that emits 3 structured images on 99.8 % of articles. Unlike `degenerate` these are NOT dropped (the rare minority is still real signal) — only DISCLOSED so the picker can flag "effectively constant in this scope". Scope-relative: a different source set with real variance clears the flag, so it reads as "this scope's sources carry little signal here", not a property of the metric. The metric analogue of ScopeAvailableMetadata.lowSignal. */
                        lowSignal?: {
                            metricName: string;
                            /** @description Number of distinct values for the metric across the scope. */
                            distinctValues: number;
                            /** @description Fraction of in-scope rows carrying the single most common value (0..1). A value near 1 means the metric is effectively constant. */
                            dominantShare: number;
                            /** @description The single most common value for the metric across the scope. */
                            dominantValue: number;
                        }[];
                        /** Format: date-time */
                        windowStart?: string;
                        /** Format: date-time */
                        windowEnd?: string;
                    };
                };
            };
            /** @description Invalid scope or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getScopeAvailableMetadata: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Available (all-source) and partial (some-source) categorical field sets. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ScopeAvailableMetadata"];
                };
            };
            /** @description Invalid scope or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetadataDistribution: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Maximum number of categories to return (ranked by article count). Default 20, clamped to [1, 200]. */
                topN?: number;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path: {
                /** @description Categorical metadata field name (e.g. `section`, `tags`, `author`). */
                field: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Categorical distribution payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CategoricalDistributionResponse"];
                };
            };
            /** @description Invalid scope, unknown probe / source, or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetadataCrossTab: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Maximum number of categories to return (ranked by article count). Default 20, clamped to [1, 200]. */
                topN?: number;
                /** @description Faceting / small-multiples filter (Phase 125a). Restricts the per-article computation to articles whose categorical metadata field `metadataFilterField` carries the value `metadataFilterValue`. Both must be supplied together; a field without a value (or vice versa) is ignored. The membership is evaluated over the same window and resolved source set as the main query. */
                metadataFilterField?: string;
                /** @description Faceting / small-multiples filter value (Phase 125a). The categorical value that `metadataFilterField` must carry for an article to be included. Both parameters must be supplied together; either alone is ignored. */
                metadataFilterValue?: string;
            };
            header?: never;
            path: {
                /** @description Categorical metadata field name (e.g. `section`, `author`). */
                field: string;
                /** @description Numeric metric name (e.g. `sentiment_score_sentiws`, `word_count`). */
                metric: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Cross-tab payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        field: string;
                        metric: string;
                        categories: {
                            value: string;
                            /**
                             * Format: int64
                             * @description Distinct in-scope articles in this category that carry the metric.
                             */
                            articles: number;
                            /**
                             * Format: double
                             * @description Mean of the metric over the articles in this category.
                             */
                            mean: number;
                            /**
                             * Format: double
                             * @description Sample standard deviation of the metric in this category (NaN/0 for a single article).
                             */
                            std: number;
                        }[];
                        /**
                         * Format: int64
                         * @description Total number of distinct category values (drives "showing top N of M").
                         */
                        distinctValues: number;
                        scope?: string | null;
                        scopeId?: string | null;
                        /** Format: date-time */
                        windowStart?: string | null;
                        /** Format: date-time */
                        windowEnd?: string | null;
                    };
                };
            };
            /** @description Invalid scope, malformed window, or a cross-frame equivalence refusal. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetadataSankey: {
        parameters: {
            query: {
                /** @description Comma-separated ordered list of categorical field names (≥2, ≤8). */
                fields: string;
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Maximum edges per consecutive field pair (default 50, clamped to [1, 200]). */
                topN?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Alluvial flow. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description The ordered field chain (layers); ≥2. */
                        fields: string[];
                        nodes: {
                            /** @description Layer-namespaced node id (`<layer>::<value>`). */
                            id: string;
                            field: string;
                            value: string;
                            layer: number;
                        }[];
                        links: {
                            /** @description Source node id. */
                            source: string;
                            /** @description Target node id. */
                            target: string;
                            /**
                             * Format: int64
                             * @description Distinct in-scope articles flowing source → target.
                             */
                            value: number;
                        }[];
                        scope?: string | null;
                        scopeId?: string | null;
                        /** Format: date-time */
                        windowStart?: string | null;
                        /** Format: date-time */
                        windowEnd?: string | null;
                    };
                };
            };
            /** @description Invalid scope/window or fewer than two fields. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getMetadataFields: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-field corpus-wide extraction status. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetadataFieldsResponse"];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getEntities: {
        parameters: {
            query?: {
                /** @description Start date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                startDate?: string;
                /** @description End date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                endDate?: string;
                /** @description Filter metrics by data source (e.g., "wikipedia") */
                source?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Filter entities by NER label (e.g., "PER", "ORG", "LOC", "MISC") */
                label?: string;
                /** @description Maximum number of results to return (default 100, max 1000) */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A list of aggregated entity results. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description The extracted entity text (e.g., "Bundesregierung", "Berlin"). */
                        entityText: string;
                        /** @description The NER label (PER, ORG, LOC, MISC). */
                        entityLabel: string;
                        /**
                         * Format: int64
                         * @description Number of occurrences within the queried time range.
                         */
                        count: number;
                        /** @description Distinct data sources that produced this entity. */
                        sources: string[];
                        /** @description Canonical Wikidata QID resolved by the Phase 118 entity-linking step, or null when no candidate above the heuristic confidence threshold was found. The link is the highest-confidence match across all occurrences of this (entityText, entityLabel) pair in the window; ties broken by sitelink count via the alias index. */
                        wikidataQid?: string | null;
                        /**
                         * Format: float
                         * @description Confidence (0.7–1.0) of the resolved link, or null when no link was established. Heuristic Tier-1.5 weights — see `aer_gold.metric_validity` for `entity_link_confidence` and WP-002 §4.2.
                         */
                        linkConfidence?: number | null;
                    }[];
                };
            };
            /** @description Missing required parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getEntityCoOccurrence: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Maximum number of co-occurrence edges to return, ranked by aggregated weight. Server clamps values outside [1, 6000] to the nearest bound. The default SVG renderer requests a small value (~60); the large-scale WebGL renderer (maximized single-cell view, Phase 125b) requests a high value. */
                topN?: number;
                /** @description Phase 125b — minimum co-occurrence weight (summed `cooccurrence_count`) for an edge to be included. The primary control against the quadratic edge "hairball" in the large-scale view: raise it to keep only the strongest pairs. 0 / absent disables the threshold. The node set is derived from the surviving edges, so this thins both edges and nodes. */
                minWeight?: number;
                /** @description Optional viewer-language code (e.g. `de`, `en`, `fr`) for the cross-lingual relabel toggle (Phase 123b). When present, each node's resolved Wikidata QID is looked up in `aer_gold.wikidata_labels` and the display label in that language is attached as `viewerLabel`. Nodes without a QID, or QIDs lacking a label in this language, keep their source surface form. Absent (the default) disables relabelling — nothing changes silently. This swaps in the per-language label Wikidata publishes for a QID; it is never a machine translation. */
                viewerLanguage?: string;
                /** @description Phase 125 — when set, each node carries `metricValue` = the mean of this per-article metric over the articles where the entity appears, so the network cell can size/colour nodes by a metric. This is the SIZE channel metric when size and colour bind to different metrics. */
                nodeMetric?: string;
                /** @description Phase 125 / ISSUE 7 — optional SECOND per-article metric for the COLOUR channel, so size (`nodeMetric`) and colour can bind to different metrics. When set and different from `nodeMetric`, each node carries `metricValueColor` = the mean of this metric. Omit (or set equal to `nodeMetric`) to colour by the same metric as size. */
                nodeColorMetric?: string;
                /** @description Phase 122d.2 — when `ghost`, each edge carries `nsSupport` = the number of its contributing articles whose timestamp is the crawler fetch time, not a real publication date (`timestamp_source='fetch_at_fallback'`). A reflexive disclosure ("this connection leans on undated articles", WP-005 §3.1) — the edges are NOT filtered out. Default off (`nsSupport`=0). */
                negativeSpaceOverlay?: "ghost";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Top-N co-occurrence subgraph. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CoOccurrenceGraph"];
                };
            };
            /** @description Invalid scope or window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    postEntityCoOccurrenceQuery: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description One or more ScopeGroups. The handler unions the resolved sources across groups and asserts the 100-source / 25-probe cap on the union (not on the individual group). */
                    scopes: {
                        probeIds: string[];
                        /** @description Empty array = all sources of the group's probes. */
                        sourceIds: string[];
                    }[];
                    /**
                     * Format: date-time
                     * @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH windowStart and windowEnd for the whole dataset (no time filter); supplying one without the other is rejected.
                     */
                    windowStart?: string;
                    /**
                     * Format: date-time
                     * @description Exclusive end of the query window (RFC 3339). Optional — see windowStart.
                     */
                    windowEnd?: string;
                    /**
                     * @description Maximum number of co-occurrence edges to return. Server clamps values outside [1, 6000] to the nearest bound (same ceiling as the GET endpoint and the cooccurrence-network config UI).
                     * @default 50
                     */
                    topN?: number;
                    /** @description Optional viewer-language code (e.g. `de`) for the cross-lingual relabel toggle (Phase 123b). When present, each node's resolved QID is looked up in `aer_gold.wikidata_labels` and the display label in that language is attached as `viewerLabel`; unlinked nodes (and QIDs lacking a label in this language) keep their source surface form. Absent = no relabelling. Swaps in the per-language Wikidata label, never a machine translation. */
                    viewerLanguage?: string;
                };
            };
        };
        responses: {
            /** @description Top-N co-occurrence subgraph for the unioned scope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CoOccurrenceGraph"];
                };
            };
            /** @description Malformed body or invalid window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Scope union exceeds the 100-source / 25-probe cap. */
            413: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Cross-language merge is unsupported for the given scope. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getTopicDistribution: {
        parameters: {
            query?: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Single scope target (probe id or source name). Required when `probeIds` and `sourceIds` are absent; optional otherwise. */
                scopeId?: string;
                /** @description Comma-separated probe IDs (e.g. `probe-0-de-institutional-web,probe-1-de-diasporic-rss`). Each probe's full source list is resolved via the Probe Registry and added to the scope union. Compatible with `scopeId` and `sourceIds` — all resolved source sets are merged and deduplicated. When `segmentBy=probe` is set, each probe forms its own independent stream in the response. */
                probeIds?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — defaults to the latest available BERTopic sweep window when omitted. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — defaults to now. */
                end?: string;
                /** @description Filter by ISO 639-1 language code (e.g., "de", "en"). */
                language?: string;
                /** @description Minimum `topic_confidence` for a row to be included in the aggregation. Defaults to 0.0 (include all assignments). */
                minConfidence?: number;
                /** @description When `true`, the outlier topic (`topic_id == -1`) is included in the response as an `uncategorised` entry. Defaults to false. */
                includeOutlier?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Topic distribution payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TopicDistributionResponse"];
                };
            };
            /** @description Invalid scope, unknown probe / source, or malformed window. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getLanguages: {
        parameters: {
            query?: {
                /** @description Start date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                startDate?: string;
                /** @description End date for the metrics time range (ISO 8601). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                endDate?: string;
                /** @description Filter metrics by data source (e.g., "wikipedia") */
                source?: string;
                /** @description Comma-separated list of source names (e.g. `tagesschau,bundesregierung`). When provided alongside or instead of `scopeId`, the sources are added to the resolved scope union. Compatible with `probeIds` — both sets are merged and deduplicated. Backward-compatible with the single `source` parameter on the flat-list endpoints: if `source` is also present the two values are unioned. */
                sourceIds?: string;
                /** @description Filter by ISO 639-1 language code (e.g., "de", "en"). */
                language?: string;
                /** @description Maximum number of results to return (default 100, max 1000) */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A list of aggregated language detection results. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description ISO 639-1 language code (e.g., "de", "en"). */
                        detectedLanguage: string;
                        /**
                         * Format: int64
                         * @description Number of documents where this language was detected as the top candidate.
                         */
                        count: number;
                        /**
                         * Format: double
                         * @description Average confidence score for this language across matched documents.
                         */
                        avgConfidence: number;
                        /** @description Distinct data sources that produced this language detection. */
                        sources: string[];
                    }[];
                };
            };
            /** @description Missing required parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSources: {
        parameters: {
            query?: {
                /** @description When `true`, returns only sources whose `silver_eligible` flag is set (Phase 103). Use this to populate the dashboard's Silver-layer source picker without surfacing ineligible sources only to refuse them later. */
                silverOnly?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The list of known sources. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Source"][];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSourceById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Source identifier — either the canonical name (e.g. `tagesschau`) or the integer source id. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Source detail. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SourceDetail"];
                };
            };
            /** @description Source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    listSilverDocuments: {
        parameters: {
            query: {
                /** @description Source identifier — canonical name or integer id. */
                sourceId: string;
                /** @description Inclusive start of the timestamp window (RFC 3339). */
                start?: string;
                /** @description Exclusive end of the timestamp window (RFC 3339). */
                end?: string;
                /** @description Page size. Server clamps to `[1, 200]`; default 50. */
                limit?: number;
                /** @description Opaque pagination token returned in the previous page's `nextCursor`. */
                cursor?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of Silver document summaries. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SilverDocumentsPage"];
                };
            };
            /** @description Invalid query parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Source is not Silver-eligible. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefusalPayload"];
                };
            };
            /** @description Source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSilverDocumentDetail: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Article identifier (deterministic SHA-256 hash assigned by the worker). */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Silver document detail. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SilverDocumentDetail"];
                };
            };
            /** @description Source is not Silver-eligible. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefusalPayload"];
                };
            };
            /** @description Article or Silver object not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSilverAggregation: {
        parameters: {
            query: {
                /** @description Source identifier — canonical name or integer id. */
                sourceId: string;
                /** @description Inclusive start of the timestamp window (RFC 3339). */
                start: string;
                /** @description Exclusive end of the timestamp window (RFC 3339). */
                end: string;
                /** @description Histogram bin count for distributional aggregation types. Server clamps to `[1, 200]`; default 30. */
                bins?: number;
            };
            header?: never;
            path: {
                /** @description The aggregation kind. Selects projection column(s) and payload shape. */
                aggregationType: "cleaned_text_length" | "word_count" | "raw_entity_count" | "cleaned_text_length_by_hour" | "word_count_by_source" | "cleaned_text_length_vs_word_count";
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Aggregation payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SilverAggregationResponse"];
                };
            };
            /** @description Invalid query parameters or unsupported aggregation type. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Source is not Silver-eligible. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefusalPayload"];
                };
            };
            /** @description Source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getProbes: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The list of active probes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Probe"][];
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getProbeDossier: {
        parameters: {
            query?: {
                /** @description Start of the article-count window (RFC 3339). When omitted the per-source `articlesInWindow` counts equal the total counts. */
                windowStart?: string;
                /** @description End of the article-count window (RFC 3339). */
                windowEnd?: string;
            };
            header?: never;
            path: {
                /** @description Canonical probe identifier. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The composite Probe Dossier payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProbeDossier"];
                };
            };
            /** @description Invalid window parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getProbeEquivalence: {
        parameters: {
            query?: {
                /** @description Optional second probe id (Phase 124). When present, equivalence is evaluated over the union of both probes' source sets, giving the cross-probe pair's valid-comparison matrix. */
                comparedTo?: string;
            };
            header?: never;
            path: {
                /** @description Probe identifier — the canonical string registered in the probe registry (e.g. `probe-0-de-institutional-web`), matching the `probeId` shape used by `/probes` and `/probes/{id}/dossier`. */
                probeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-metric equivalence availability for the probe. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProbeEquivalenceSummary"];
                };
            };
            /** @description No probe with that id is registered. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getProbeLeadLag: {
        parameters: {
            query: {
                /** @description Compared probe id (e.g. `probe-1-fr-institutional-web`). */
                comparedTo: string;
                /** @description Inclusive start of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                start?: string;
                /** @description Exclusive end of the query window (RFC 3339). Optional — omit BOTH start and end for the whole dataset (no time filter); supplying one without the other is rejected. */
                end?: string;
                /** @description Symmetric lag bound in hours (default 168 = ±7 days). */
                maxLagHours?: number;
            };
            header?: never;
            path: {
                /** @description Reference probe id (e.g. `probe-0-de-institutional-web`). */
                probeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Lead-lag cross-correlation for the probe pair. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description The reference probe (the path `probeId`). */
                        referenceProbe: string;
                        /** @description The compared probe (`comparedTo`). */
                        comparedProbe: string;
                        /**
                         * @description The compared signal. Phase 124 ships `publication_activity` only.
                         * @example publication_activity
                         */
                        signal: string;
                        /** @description Symmetric lag bound; `points` span -maxLagHours..+maxLagHours. */
                        maxLagHours: number;
                        /**
                         * Format: int64
                         * @description Overlapping hourly buckets at lag 0 (a sample-size proxy).
                         */
                        bucketCountAtZero?: number;
                        /** @description The equivalence grant authorising this comparison, for the methodology banner. Server-authoritative so the UI never asserts an ungranted claim. */
                        grant: {
                            /** @example temporal */
                            level: string;
                            /** @example WP-004 Appendix B */
                            workingPaperAnchor: string;
                            /** @description Concise grant rationale (from metric_equivalence.notes). */
                            notes?: string;
                            /** @description equivalence_reviews.id of the full review record. */
                            validatedBy?: string | null;
                        };
                        /** @description One entry per integer lag in [-maxLagHours, +maxLagHours]. */
                        points: {
                            lagHours: number;
                            /**
                             * Format: double
                             * @description Pearson correlation at this lag; null when too few overlapping buckets or zero variance.
                             */
                            correlation: number | null;
                        }[];
                        /** @description Lag of maximum correlation; null when no lag has a defined correlation. */
                        peakLagHours?: number | null;
                        /**
                         * Format: double
                         * @description The maximum correlation across all lags.
                         */
                        peakCorrelation?: number | null;
                    };
                };
            };
            /** @description Invalid parameters, identical probes, or a methodological refusal — the probe pair lacks a temporal-level equivalence grant across both languages. A refusal carries the `gate`, `workingPaperAnchor`, and `alternatives` fields. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description The reference or compared probe id is not registered. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getProbeMetadataCoverage: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Canonical probe identifier. */
                probeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The probe's metadata-coverage payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetadataCoverageResponse"];
                };
            };
            /** @description Probe not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSourceArticles: {
        parameters: {
            query?: {
                start?: string;
                end?: string;
                /** @description ISO 639-1 language filter applied against the top language detection result. */
                language?: string;
                /** @description Substring match (case-insensitive) against any extracted entity for the article. Useful for narrowing the Dossier list to "articles that mention X." */
                entityMatch?: string;
                /** @description Filter by sentiment band over the SentiWS score (negative ≤ -0.05, neutral in (-0.05, 0.05), positive ≥ 0.05). */
                sentimentBand?: "negative" | "neutral" | "positive";
                limit?: number;
                /** @description Opaque pagination cursor from a previous response's `nextCursor`. */
                cursor?: string;
                /** @description Phase 122d.1 — when `true`, each item gains `chainLength`, `hasHeadlineChange`, and `latestRevisionAt` fields drawn from `aer_gold.article_revisions`. Server-side cost is a thin LEFT JOIN; rows with no revisions get null fields and the badges hide in the UI. Default `false` so existing callers receive the legacy shape. */
                includeRevisions?: boolean;
            };
            header?: never;
            path: {
                /** @description Source identifier — either the integer `sources.id` or the canonical source name. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated article listing. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ArticlesPage"];
                };
            };
            /** @description Invalid query parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSourceMetadataCoverage: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Source identifier — either the canonical name (e.g. `tagesschau`) or the integer source id. */
                sourceId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The source's metadata-coverage payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetadataCoverageResponse"];
                };
            };
            /** @description Source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getSourceDiscoveryCoverage: {
        parameters: {
            query?: {
                /** @description Trailing window for the average + underflow computation (defaults to the probe's `time_window_days`; clamped to [1, 365]). */
                windowDays?: number;
            };
            header?: never;
            path: {
                /** @description Source identifier — either the canonical name (e.g. `tagesschau`) or the integer source id. */
                sourceId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The source's discovery-coverage payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DiscoveryCoverageResponse"];
                };
            };
            /** @description Source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getArticleDetail: {
        parameters: {
            query?: {
                /** @description Metric whose aggregation group is consulted for the k-anonymity gate. Defaults to `word_count` — the metric every processed document contributes to, so the gate degenerates to "minimum document count for the article's source/window." */
                metricName?: string;
            };
            header?: never;
            path: {
                /** @description SHA-256 article_id. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Article detail payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ArticleDetail"];
                };
            };
            /** @description K-anonymity gate not satisfied — methodological refusal. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefusalPayload"];
                };
            };
            /** @description Article not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getArticleRevisions: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description SHA-256 article_id. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Article revision chain. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ArticleRevisionsResponse"];
                };
            };
            /** @description Silver-eligibility gate refusal. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefusalPayload"];
                };
            };
            /** @description Article not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getArticleRevisionDiff: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description SHA-256 article_id. */
                id: string;
                /** @description Zero-based index of the LATER snapshot in the pair. `0` is the chain-head pair (current Silver → Wayback[0], Phase 122d.1 BUG-11 amendment); values > 0 compare consecutive Wayback snapshots. */
                revisionIndex: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The paragraph-aligned diff payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ArticleRevisionDiff"];
                };
            };
            /** @description Silver-eligibility gate refusal. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefusalPayload"];
                };
            };
            /** @description Article unknown, `revisionIndex` out of range, or diff not yet computed by the worker sweep. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getRevisionActivity: {
        parameters: {
            query: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Identifier of the scope target. For `scope=probe`, a probe id (e.g. `probe-0-de-institutional-web`); for `scope=source`, a source name (e.g. `tagesschau`). Required. */
                scopeId: string;
                /** @description Inclusive start of the analysis window (RFC 3339). */
                startDate?: string;
                /** @description Exclusive end of the analysis window (RFC 3339). */
                endDate?: string;
                /** @description Aggregation grain. `snapshot` collapses to a single bucket spanning the whole window (Aleph cell); `daily` / `weekly` / `monthly` bucket the window on that grain (Episteme cell). */
                resolution?: "snapshot" | "daily" | "weekly" | "monthly";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Aggregated revision-activity payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RevisionActivityResponse"];
                };
            };
            /** @description Invalid scope/window/resolution parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getRevisionDiscourseShift: {
        parameters: {
            query: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Identifier of the scope target. For `scope=probe`, a probe id (e.g. `probe-0-de-institutional-web`); for `scope=source`, a source name (e.g. `tagesschau`). Required. */
                scopeId: string;
                /** @description Inclusive start of the analysis window (RFC 3339). */
                startDate?: string;
                /** @description Exclusive end of the analysis window (RFC 3339). */
                endDate?: string;
                /** @description Aggregation grain. `snapshot` collapses to a single bucket spanning the whole window; `daily` / `weekly` / `monthly` bucket the window on that grain (the Episteme trajectory). */
                resolution?: "snapshot" | "daily" | "weekly" | "monthly";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Aggregated discourse-shift payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RevisionDiscourseShiftResponse"];
                };
            };
            /** @description Invalid scope/window/resolution parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getRevisionEditClusters: {
        parameters: {
            query: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Identifier of the scope target. For `scope=probe`, a probe id (e.g. `probe-0-de-institutional-web`); for `scope=source`, a source name (e.g. `tagesschau`). Required. */
                scopeId: string;
                /** @description Inclusive start of the analysis window (RFC 3339). */
                startDate?: string;
                /** @description Exclusive end of the analysis window (RFC 3339). */
                endDate?: string;
                /** @description The time-bucket grain coordinated edits are grouped within. */
                resolution?: "snapshot" | "daily" | "weekly" | "monthly";
                /** @description Minimum distinct co-editing sources for a (bucket, entity) to qualify as a cluster. Clamped to [2, 10]; default 2. */
                minSources?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Coordinated-edit-cluster payload. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RevisionEditClustersResponse"];
                };
            };
            /** @description Invalid scope/window/resolution parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getRevisionsArticles: {
        parameters: {
            query: {
                /** @description Scope of the query. `probe` resolves the scopeId against the probe registry and applies the probe's full source list. `source` filters by a single source. Defaults to `probe` per Design Brief §4.2.4. */
                scope?: "probe" | "source";
                /** @description Identifier of the scope target. For `scope=probe`, a probe id (e.g. `probe-0-de-institutional-web`); for `scope=source`, a source name (e.g. `tagesschau`). Required. */
                scopeId: string;
                /** @description Inclusive start of the analysis window (RFC 3339). */
                startDate?: string;
                /** @description Exclusive end of the analysis window (RFC 3339). */
                endDate?: string;
                /** @description When `true`, only return articles whose chain contains at least one revision with `headline_changed=true`. The default (unset / `false`) returns every article with ≥ 1 revision. */
                hasHeadlineChange?: boolean;
                /** @description Minimum chain length per article. Defaults to 1 (any article with at least one revision); raise to filter for articles edited multiple times. */
                minChainLength?: number;
                /** @description Page size; max 200. */
                limit?: number;
                /** @description Opaque cursor token from the previous response. */
                cursor?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated article list. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RevisionsArticlesPage"];
                };
            };
            /** @description Invalid scope/window/filter parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Probe or source not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
    getHealthz: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Service is alive. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: string;
                    };
                };
            };
        };
    };
    getReadyz: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Service is ready to serve traffic. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: string;
                    };
                };
            };
            /** @description Service is not ready — one or more dependencies are unavailable. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: string;
                    };
                };
            };
        };
    };
    getContent: {
        parameters: {
            query?: {
                /** @description The language of the content to return. Defaults to "en". */
                locale?: "en" | "de";
            };
            header?: never;
            path: {
                /** @description The category of the entity. */
                entityType: "metric" | "probe" | "source" | "discourse_function" | "refusal" | "view_mode" | "empty_lane" | "open_research_question" | "primer";
                /** @description The canonical identifier of the entity (e.g., "sentiment_score"). */
                entityId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Content found for the requested entity and locale. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ContentResponse"];
                };
            };
            /** @description Invalid entityType value. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description No content found for the requested entity or locale. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
            /** @description Internal server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description A human-readable error message. */
                        message: string;
                        /** @description Phase 115: when the 400 represents a methodological refusal (e.g. cross-frame equivalence gate), this field carries the machine identifier of the gate that fired. Same value space as `RefusalPayload.gate` (currently `metric_equivalence` is the only value used at this status). Absent for plain validation errors. */
                        gate?: string | null;
                        /** @description Phase 115: anchor into the methodological surface (e.g. `WP-004#section-5.2`) when the 400 is a methodological refusal. */
                        workingPaperAnchor?: string | null;
                        /** @description Phase 115: concrete user-actionable alternatives when the 400 is a methodological refusal — e.g. drop normalization to Level 1, constrain scope to one cultural frame, use deviation labelling. */
                        alternatives?: string[] | null;
                    };
                };
            };
        };
    };
}
