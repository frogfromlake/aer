node_modules/
.svelte-kit/
build/
dist/
.env
.env.*
!.env.example
.vite/
.turbo/
coverage/
playwright-report/
test-results/
# Phase 128 — Lighthouse CI output (report + autorun temp dir).
lhci-report/
.lighthouseci/
*.log
.DS_Store
# Phase 144 — Paraglide compiled message functions (generated by
# `pnpm run messages`; the Vite plugin also emits them on dev/build). The
# source of truth is messages/{en,de}/*.json + project.inlang/, both committed.
src/lib/paraglide/
