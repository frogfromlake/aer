nats-py==2.7.2
structlog==24.1.0
python-dotenv==1.0.1
minio==7.2.7
clickhouse-connect==0.7.8
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp==1.24.0
pydantic==2.6.3
# NOTE: psycopg2-binary is for development/CI only. It bundles its own libpq
# and is not recommended for production. For production builds, use 'psycopg2'
# (requires libpq-dev to be installed in the Dockerfile).
psycopg2-binary==2.9.9

# --- Observability ---
prometheus-client==0.20.0

# --- Resilience ---
tenacity==8.2.3

# --- Testing ---
pytest==8.0.2
pytest-asyncio==0.23.5
testcontainers==4.8.1

# --- Security Overrides (Pinned to fix CVEs) ---
jaraco.context>=6.1.0
protobuf>=5.29.6
wheel>=0.46.2