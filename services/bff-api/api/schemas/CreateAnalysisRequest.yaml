type: object
required: [name, state]
properties:
  name: { type: string, minLength: 1 }
  description: { type: string }
  state: { type: string, description: The serialized Workbench URL query to persist. }
