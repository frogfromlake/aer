type: object
description: >
  Password change for an authenticated user who knows their current password
  (ADR-040). No email round-trip. Other sessions are invalidated on success.
required:
  - currentPassword
  - newPassword
properties:
  currentPassword:
    type: string
  newPassword:
    type: string
    minLength: 12
