type: object
description: >
  Self-service password-reset request (ADR-040). The response is always 202
  regardless of whether the email maps to an account — no user enumeration.
required:
  - email
properties:
  email:
    type: string
    format: email
