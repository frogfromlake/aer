name: yMetric
in: query
description: >
  Metric name bound to the scatter's Y position channel (e.g.
  `sentiment_score_sentiws`). Required. Only articles carrying both
  `xMetric` and `yMetric` in the window contribute a point.
required: true
schema:
  type: string
  minLength: 1
