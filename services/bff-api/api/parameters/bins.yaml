name: bins
in: query
description: >
  Number of histogram bins to request for the distribution. Server clamps
  values outside [1, 200] to the nearest bound.
required: false
schema:
  type: integer
  default: 30
  minimum: 1
  maximum: 200
