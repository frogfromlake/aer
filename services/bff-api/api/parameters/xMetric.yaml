name: xMetric
in: query
description: >
  Metric name bound to the scatter's X position channel (e.g. `word_count`).
  Required. Only articles carrying both `xMetric` and `yMetric` in the window
  contribute a point.
required: true
schema:
  type: string
  minLength: 1
