-- Migration 009: Multi-resolution materialized views (deferred preparation).
-- Phase 66: Multi-Resolution Temporal Framework (WP-005 §5.4).
--
-- These materialized views pre-aggregate aer_gold.metrics into hourly, daily,
-- and monthly buckets so that long-range queries skip the per-row aggregation
-- cost. They are intentionally LEFT COMMENTED OUT — query-time aggregation via
-- toStartOfHour/Day/Month in the BFF GetMetrics handler is sufficient at
-- current ingestion volumes (Probe 0: ~50 articles/day).
--
-- ACTIVATION CRITERIA (WP-005 §5.4):
--   - p95 GetMetrics latency exceeds the 1.5 s SLO at any single resolution; OR
--   - the BFF row-cap multiplier (storage.Resolution.rowLimitMultiplier) starts
--     truncating result sets for daily/weekly/monthly windows; OR
--   - corpus growth pushes raw metrics scans above 10⁸ rows per typical request.
--
-- ACTIVATION PROCEDURE:
--   1. Drop the leading "-- " from each statement below.
--   2. Author a new migration (do NOT edit this file in place — Phase 66
--      records the deferred state and is part of the audit trail).
--   3. Backfill historical buckets via INSERT INTO ... SELECT before flipping
--      the BFF query layer to read from the views.
--   4. Update Arc42 §8.6 (multi-resolution downsampling strategy) and §8.8
--      (tiered retention) to reflect that the view is live.

-- CREATE MATERIALIZED VIEW IF NOT EXISTS aer_gold.metrics_hourly
-- ENGINE = AggregatingMergeTree
-- ORDER BY (bucket, source, metric_name)
-- TTL bucket + INTERVAL 365 DAY
-- AS SELECT
--     toStartOfHour(timestamp) AS bucket,
--     source,
--     metric_name,
--     avgState(value)          AS value_avg_state,
--     countState()             AS sample_count_state
-- FROM aer_gold.metrics
-- GROUP BY bucket, source, metric_name;

-- CREATE MATERIALIZED VIEW IF NOT EXISTS aer_gold.metrics_daily
-- ENGINE = AggregatingMergeTree
-- ORDER BY (bucket, source, metric_name)
-- TTL bucket + INTERVAL 1825 DAY
-- AS SELECT
--     toStartOfDay(timestamp) AS bucket,
--     source,
--     metric_name,
--     avgState(value)         AS value_avg_state,
--     countState()            AS sample_count_state
-- FROM aer_gold.metrics
-- GROUP BY bucket, source, metric_name;

-- CREATE MATERIALIZED VIEW IF NOT EXISTS aer_gold.metrics_monthly
-- ENGINE = AggregatingMergeTree
-- ORDER BY (bucket, source, metric_name)
-- AS SELECT
--     toStartOfMonth(timestamp) AS bucket,
--     source,
--     metric_name,
--     avgState(value)           AS value_avg_state,
--     countState()              AS sample_count_state
-- FROM aer_gold.metrics
-- GROUP BY bucket, source, metric_name;
